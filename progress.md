# Progress: 햄스터S 말판 제작 웹앱 (hamsterCreator)
Last updated: 2026-09-16

---

# 인수인계: 이어받는 사람이 먼저 읽을 사항

> 이 문서는 여러 세션의 구현·검증 이력을 담습니다. 2026-09-14 실제 코드와 Git을 대조해 상단/하단의 상태 불일치를 정리했습니다.
> `Completed`는 개별 구현 단계의 이력이지 프로젝트 전체 완료 선언이 아닙니다. 현재 상태는 `Current Status`, 미완료 작업은 `Next Steps`와 `Risks`를 함께 보십시오.

사용자는 20년차 중학교 정보 교사이고, 이 앱으로 수업용 말판을 만듭니다. 사용자가 코드를 직접 열어 고칠 예정이므로 주석이 곧 설명서입니다.
기준 문서는 `docs/03_prd.md`입니다. 수치가 명시된 부분은 그대로 구현하고, 코드와 어긋나면 양쪽을 함께 고치십시오.

## 작업 규칙

사용자가 직접 지시한 사항이므로 임의로 바꾸지 마십시오.

1. 분석, 설계, PRD, 검토, 구현 순서를 지킵니다. 코드부터 쓰지 않습니다.
2. 서브에이전트는 가급적 사용하지 않고 현재 담당자가 직접 작업합니다(사용자 최신 지시).
3. 한 단계를 마칠 때마다 이 문서에 기록하고 한 줄로 보고합니다.
4. 주석은 한국어로 쓰되, 무엇을 하는지보다 왜 그렇게 했는지를 설명합니다.
5. 화면은 시각 요소가 우선입니다. 글자만 빽빽해서는 안 되고, 10초 안에 이해되어야 합니다.
6. 말판 제작에 걸리는 시간은 묻지도 표시하지도 않습니다.
7. 배포와 푸시는 모든 작업을 마친 뒤에 한다는 사용자 지시를 유지합니다. 실제 커밋·원격 상태는 재개 시 Git으로 확인하고, 과거 기록만으로 미커밋·미푸시라고 단정하지 마십시오.
8. 사용량 한도가 10퍼센트 남으면 중단하고 이 문서에 기록합니다.
9. 사용자 허락 없이 폴더 내 파일이나 코드를 임의 수정·삭제하지 않습니다. 코딩 요청은 먼저 분석 결과를 보고하고, 사용자가 구현을 요청하면 코딩합니다.
10. 한도·토큰으로 중단된 작업을 재개할 때는 단순히 이어 쓰지 않습니다. 현재 산출물의 실제 상태부터 점검하고 완료/미완료를 재구성한 뒤 미완료 부분만 수행합니다. 중단으로 생긴 누락·중복·불일치도 확인하고, 사용자 지시 범위에서 수정합니다.

## 바꾸면 안 되는 값

공식 배포 PDF 23종을 실측한 값이므로 반올림하거나 조정하지 마십시오.

- 칸 간격은 50밀리미터, 격자선 폭은 8밀리미터입니다.
- 격자선이 칸의 중심을 지나가므로, 셀 (c, r)의 중심이 곧 그래프 노드이고 그 위치는 `(c×50+25, r×50+25)`밀리미터입니다.
- 여러 장으로 나눌 때 이음매는 칸 경계에 둡니다. 종이 단차가 선 아래에 놓이면 바닥 센서가 선을 놓칩니다.

## 이미 겪은 문제

| 지점 | 내용 |
|---|---|
| 자산 파일 이름 | `adv-*.png`를 광고 차단 확장이 막아 앱 전체가 흰 화면이 되었습니다. `import.meta.glob`에 `eager: true`를 주어 png가 모듈로 로드되기 때문입니다. 이름에 `ad`, `adv`, `banner`, `track` 등을 넣지 마십시오. |
| 개발 서버 | `host`를 지정하지 않으면 윈도우에서 IPv6에만 붙어 크롬이 접속하지 못합니다. 현재 `host: true`이고 포트는 5173으로 고정했습니다. 개발용 `base`는 `'/'`이며 배포 빌드에서만 `/hamsterCreator/`를 씁니다. |
| 폰트 | Pretendard 동적 서브셋을 쓰면 단일 HTML이 31메가바이트가 됩니다. 고정 서브셋 3종만 씁니다. |
| 격자선 끝 | `lineCap`이 `'round'`면 선이 4밀리미터 길어져 인쇄 치수가 어긋납니다. `'butt'`를 쓰고 교차점만 사각형으로 메웁니다. |
| cells 배열 | 인덱스가 `r×cols + c`라서 cols가 바뀌면 공식 자체가 달라집니다. 크기를 바꿀 때는 (열, 행) 좌표를 기준으로 다시 배치해야 하며, 배열을 자르면 타일이 전부 밀려납니다. |
| 선택 상태 | Prop과 Label에는 id가 없어 배열 인덱스로 가리킵니다. 삭제한 직후 반드시 `setSelection(null)`을 호출하십시오. |
| 실행취소 | 드래그는 `toolInteractions.ts`가 제스처 단위로 쌓고, 단발 편집은 `editorStore.commitDoc()`을 씁니다. 두 방식을 섞으면 단계가 어긋납니다. |
| PDF | `window.print()`는 브라우저가 임의로 축소하므로 쓰지 않습니다. `pdf-lib`으로 밀리미터를 직접 지정하십시오. |

## 검증 방법

브라우저 자동화 환경에서 창 배율이 매번 달라지기 때문에 좌표를 기준으로 클릭하는 방식은 믿을 수 없습니다. localStorage의 `hamsterS.drafts.v1`을 읽어 실제 문서와 대조하거나, `.hsmap.json`을 창에 떨어뜨려 원하는 상태를 주입하는 편이 확실합니다. React가 관리하는 입력 요소는 네이티브 setter로 값을 넣은 뒤 `focusout` 이벤트를 발생시켜야 값이 확정됩니다.

## 가장 먼저 할 일

먼저 최신 사용자 규칙과 실제 코드·Git·이 문서를 대조해 완료/미완료 상태를 재구성하십시오. M4i의 시작 화면 프리셋 8종은 이미 코드에 있으므로 다시 구현하지 않습니다. 다음 작업은 남은 코드 위험·PRD 요구사항 정합성 점검과 v1 성공 기준 검증입니다. 새 코딩은 분석 보고 후 사용자 요청을 받아 수행합니다.

---

## Goal
- 햄스터S 말판·라인트레이서 트랙을 화면에서 배치하고, 원하는 용지로 나눠 인쇄할 수 있는 정확한 축척의 PDF와 정답 코드를 뽑아주는 교사용 웹 도구
- v1 완료 기준: PRD §1.2의 S1~S4, S6, S7 전부 통과

## Current Status
- Status: **2026-09-16 실배포 전 최종 검토 완료. 배포 차단 항목 5건이 남아 아직 배포하지 않음**
- Current focus: **배포 차단 0~5번 + 배포 후 수정 6·7번 완료. 다음 = 사용자 확인 후 커밋·배포 여부 결정**
- Git 확인 기준(2026-09-14): M4a~M4i 변경은 `b7aa02a`에 커밋됐고, 최신 HEAD는 `f70a950`입니다. 문서 최신화 전 작업 트리는 깨끗했습니다.
- 로컬 `main`과 로컬 원격 추적 참조 `origin/main`은 같은 커밋이며 ahead/behind 표시는 없습니다. 이번 점검에서 fetch나 원격 서비스 조회는 하지 않았으므로 원격 서버의 최신 상태·푸시 수행 이력·Pages 배포 성공 여부는 별도 확인 대상입니다. 이 문서 수정은 아직 커밋하지 않았습니다.
- 기존 단계의 빌드·브라우저 QA 기록은 보존하되 이번 상태 점검에서 다시 실행한 결과로 취급하지 않습니다. 전체 PRD 요구사항 충족·실기 성공도 이번 코드 확인만으로 확정하지 않습니다.

### 재개할 때 이것부터
1. 실제 상태 점검 → 완료/미완료 재구성 → 분석 보고를 먼저 합니다. 브라우저 확인이 필요하면 `npm install` (필요 시) → `npm run dev` → **`http://localhost:5173`** (개발 서버는 루트에서 열립니다. 하위 경로 불필요)
2. 지금까지 만든 것: 시작 화면(프리셋 8종·파일 열기·초안 복구) → 편집기 레이아웃 + 캔버스(팬·줌·눈금자·미니맵) + 팔레트(타일 35종·아이콘 8종) + 도구 11종 + 실행취소 + 인스펙터 + 검증 + PDF 출력·타일링 + 정답 생성
3. 편집·출력의 핵심 흐름과 M4a~M4i 구현은 코드에 있습니다. 남은 코드 위험·PRD 항목 점검, 실제 파일 저장·인쇄·로봇 주행·첫 사용자 관찰, 배포 상태 확인은 별개입니다. `Next Steps`가 비어 있지 않으므로 전체 완료라고 보고하지 않습니다.
4. 다음 작업은 아래 `Next Steps`의 1번부터
- Repo: `https://github.com/t777-kaewoong/hamsterCreator` (Public. 원격 연결됨. 배포는 모든 작업 완료 후로 미룸)
- 배포 설정 URL: `https://t777-kaewoong.github.io/hamsterCreator/` (이번 점검에서 접속·배포 성공 여부 확인 안 함)

### 2026-09-14 재개 상태 점검

- 실제 코드 확인: `trackPresets.ts`의 S 7정점·U 확대 좌표, `validate.ts`의 공식 타원 예외, `goalNames.ts`와 도착지 이름 입력/보존 경로, `start/presets.ts`의 프리셋 8종이 존재합니다. M4f~M4i를 미구현으로 되돌리거나 중복 구현하지 않습니다.
- 기록 결함: 상단 날짜·M4h·프리셋 5종·미커밋 표기는 하단 M4i 이력과 실제 코드/Git에 맞지 않았습니다. 이번 문서 최신화에서 바로잡았습니다.
- 아직 남은 코드 항목: `draft.ts`는 세션별 초안 슬롯을 계속 사용하고, 기본 `createFullGridMap`의 `stubs`는 비어 있습니다. 미결 위험과 PRD 권장 항목을 전체 완료 선언에서 제외합니다.
- 확인 범위: 소스·기존 이력·Git 참조 대조입니다. 전체 PRD 감사, 빌드/브라우저 재검증, 물리 출력/실주행, 원격 배포 확인은 이번 점검에서 수행하지 않았습니다.

### 2026-09-16 실배포 전 최종 검토 (읽기 전용 검증)

빌드·타입·dist 정적 분석·브라우저 스모크·생성 PDF 바이트 해부를 이번 세션에서 직접 실행했습니다.
과거 단계의 QA 기록은 이번 판정에 옮겨 적지 않았습니다. 물리 검증(인쇄·실측·조립·주행·신규 사용자 관찰)은 수행 불가라 사용자 확인 대기로 남깁니다.

**이번에 직접 확인해 통과한 것**

- `npx tsc -b --force` 오류 0건. `npm run build` 성공(4.93초), `npm run build:single` 성공.
- dist 38개 파일 / 5.20MB. 렌더를 막는 자산은 js gzip 628KB + css gzip 9KB = 약 637KB뿐입니다.
  폰트 3종(804KB)은 `font-display: swap`이라 첫 페인트를 막지 않고, PDF 전용 TTF 2.67MB는
  PDF 생성 시에만 fetch합니다(`pdfResources.ts`). → NFR-2는 용량 근거로 충족 가능(학교망 실측은 사용자 몫).
- 배포 경로 정합성: dist의 index.html·CSS url()·동적 자산이 전부 `/hamsterCreator/` 접두어를 씁니다.
  JS 안의 `/src/assets/tiles/*.png` 34건은 `import.meta.glob`의 **키**일 뿐 URL로 쓰이지 않습니다
  (`src/lib/tiles/catalog.ts`가 키에서 id만 뽑고 값을 URL로 씁니다). **흰 화면 위험 없음.**
- 타일 35종 전원 생존(30종 해시 파일 + 4KB 미만 5종 data URI 인라인), 아이콘 8종은 전부 data URI.
  빌드 후 파일명에 광고 차단기 금칙어 0건. 라우터가 없어 Pages 새로고침 404도 불가능합니다.
- 브라우저 스모크 전 구간 통과, 콘솔 오류 0건·네트워크 실패 0건:
  프리셋 → 타일 배치(초안에 기록 확인) → `.hsmap.json` 드롭 열기 → 출력 계획기 → 지도 PDF → 정답 → 정답지 PDF.
- NFR-9: 프리셋 클릭 → 편집 가능 캔버스까지 **86ms** (기준 0.5초).
- NFR-3: A4 4장 PDF 생성 **723ms** (기준 10초). 정답지 PDF 429ms.
- NFR-10: 편집기 기본 화면 문장 전수 확인, 문장당 최대 7어절로 초과 없음.
- **생성 PDF 바이트 실측** — 5쪽(4시트 + 조립 안내도), MediaBox 전부 297.000 x 210.000mm,
  격자선 폭 `22.67716535433071 w` = **8.0000mm**, 노드 열 간격 **정확히 50.000mm**,
  50mm 눈금자 실측 길이 **50.000mm**, 시트 오프셋 차이 가로 250mm(5칸)·세로 200mm(4칸)
  → **이음매가 셀 경계에 정확히 놓임(FR-6.3)**.
  한글은 Type0/CIDFontType2/Identity-H + FontFile2 전체 임베드이고,
  정답 PDF 38개 문자열 **756글리프 중 .notdef(0000) 0개** → 글리프 누락 없음(FR-6.9).
- FR-7 정답 경로 손검산 일치: (0,7)N → (9,0) → (5,3) = 9+7+4+3 = **23칸 · 회전 4회**.
- Git: `git fetch` 후 로컬 `main` == `origin/main` (0/0). 미커밋은 `progress.md` 하나.
  `tokenSave.txt`·교과서 PDF·`tmp/`·`.claude/`는 **과거 커밋에도 없음**(히스토리 깨끗).
  `npm ci --dry-run` 통과.

**미결 위험 재판정 (progress.md Risks 대조)**

| 위험 | 판정 | 근거 |
|---|---|---|
| 초안 슬롯 덮어쓰기 | **여전함 — 브라우저에서 재현 성공** | `draft.ts:37`(세션당 id 1개), `draft.ts:133`(같은 id 레코드 제거 후 삽입) |
| 이탈 경고(NFR-5) | **여전함(부분 미구현)** | `beforeunload` 리스너 0건. 앱 내 뒤로가기 모달만 존재(`TopBar.tsx:120`) |
| Prop/Label 삭제 후 `setSelection(null)` | **해결됨** | 삭제 경로 전부 호출(`Inspector.tsx:132/183/373`, `toolInteractions.ts:1151/1302`) |
| 드래그 vs 단발 편집 실행취소 | **설계상 남아 있음(사고는 미발견)** | `commitDoc`은 자동 push, 드래그는 수동 push/drop. 규율로만 유지됨 |
| `cells` 인덱스 재배치 | **오해였음 / 해결됨** | `resize.ts:7-13`이 (열,행) 기준 재배치 + edges·stubs·markers 정리. 호출부 2곳 모두 이 함수만 사용 |
| PDF 치수 상수 일치 | **일치** | 위 실측치 참조 |

**초안 덮어쓰기 재현 경로 (이번 세션에서 실제로 재현)**

1. 프리셋 `A4 기본` 열기 → 타일 1개 배치 → 초안 슬롯 `draft-...-p1s7ym` 생성(채워진 칸 1개)
2. 뒤로가기 → "저장하지 않고 나가기"
3. 다른 프리셋 `빈 격자` 열기 → 열을 6으로 변경
4. 결과: **슬롯 수 1개 그대로, id 동일, 채워진 칸 0개. 1번 맵 초안 소실.**
   `MAX_DRAFTS = 5`는 브라우저를 다시 연 횟수만 세고 세션 내 문서 전환은 세지 않습니다.

**이번 검토에서 새로 발견한 것**

- **타일 7종이 격자선 아래로 깔림** — `drawBoard.ts:128`·`generateMapPdf.ts:229`가 `kind === 'object'`만
  선 위에 그립니다. `block` 7종(`forest-bush` `forest-berry` `ice-rock` `ice-floor` `ice-footprint`
  `ice-snowwall` `dino-grassrock`)이 전부 선 아래로 깔립니다. 사용자가 수풀에서 발견해 보고한 건과 동일 원인.
  주의: `ice-floor`·`ice-footprint`는 이름대로 바닥이라 규칙을 통째로 `kind !== 'floor'`로 바꾸면 안 됩니다.
- **출발·도착 이름표가 검은 선 위 검은 글씨** — 마커는 원 안만 흰 바탕으로 가리고
  이름표는 가리지 않습니다(`drawBoard.ts:321`, PDF는 `generateMapPdf.ts:387`의 `onLine: false`).
  마커는 노드(=칸 중심)에 놓이고 세로 격자선이 그 아래를 지나므로 **인쇄물에서도 안 보입니다.**
  FR-4.2의 "선 위 흰 글씨 모드"가 이미 있는데 마커 이름표만 안 쓰고 있습니다.
- **50mm 눈금자·"100% 인쇄" 안내가 프린터 인쇄 불가 여백에 위치** —
  `generateMapPdf.ts:451-456` 눈금자 y=1.4mm(눈금 0.7~2.7mm, 글자 0.65mm),
  `generateMapPdf.ts:438` 안내 문구는 상단에서 3.5mm. 일반 A4 프린터 상하 여백은 4.2~5mm라
  **둘 다 잘려 안 나올 가능성이 높습니다.** 말판 자체(가장 바깥 잉크가 종이 끝에서 21mm)는 무사합니다.
- **FR-2.4 경계 진입로 UI 미구현 확정** — 렌더·직렬화·프리셋 2종은 stub을 다루지만
  추가/제거 UI가 없습니다(`toolInteractions.ts`·`Inspector.tsx`에 stub 코드 0건).
  `createFullGridMap`의 `stubs`는 여전히 `[]`(`factory.ts:61`).
- **PWA/오프라인(NFR-7) 미구현 확정** — service worker·manifest·관련 플러그인 없음.
- **`robots.txt`가 dist에 안 들어감** — 루트에 있고 `public/`에 없어 복사되지 않습니다.
  내용도 `Allow: /`라 `index.html`의 `noindex` 메타와 방향이 반대입니다.
- **개발용 링크가 운영 화면에 노출** — `EditorLayout.tsx:137` "컴포넌트 카탈로그 (개발용)"에 DEV 분기 없음.
- **파일을 새로 열어도 직전 문서의 "저장 안 됨" 상태가 남음** — `App.openDoc`이 `setDoc`만 호출하고
  `saveState`를 초기화하지 않습니다.
- **공개 저장소 권리 문제** — `말판/` 아래 로보메이션 공식 PDF 23종(4.6MB)이 추적 중이고,
  내장 아트 35종·아이콘 8종에 라이선스·출처 고지 파일이 없습니다(폰트만 `LICENSE.txt` 있음).
  "무료 배포"는 추출·재가공·재배포 허락과 다르므로 확인이 필요합니다.
- `rules.md.txt` 0바이트 빈 파일이 추적 중. README 없음. 파비콘 없음.
- 워크플로 `concurrency.cancel-in-progress: true` — 연속 푸시 시 앞 배포가 중간에 취소될 수 있습니다
  (GitHub 공식 Pages 예시는 `false` 권장).
- 워크플로 `node-version: 20`은 vite 8·plugin-react 6의 `^20.19.0 || >=22.12.0`을 현재는 만족하지만
  20.19 미만으로 고정하면 깨집니다.

**확인하지 못한 것 (사용자 실기 필요)**

- S1 p190 재현 / S2 피치 자 실측 / S3 A4 4장 인쇄·재단·조립 / S4 실물 완주 / S6 라인트레이싱 / S7 신규 사용자 3명 관찰
- 생성 PDF를 실제로 열어 한글 라벨·조립 안내도 배치를 눈으로 확인 (글리프 누락 0건은 확인, 배치·겹침은 미확인)
- FR-8.3/8.4 사용자 이미지 업로드 → 저장 → 복구 실파일 왕복
- 실제 마우스로 드래그·드롭·Shift 자유 배치
- GitHub Settings > Pages > Source = "GitHub Actions" 설정 여부, Actions 실행 이력 (이 PC에 `gh` 없음)
- 학교망 첫 화면 로딩 실측(NFR-2)
- `말판/` PDF와 내장 아트의 재배포 허용 여부

**사용자 확인 대기 질문 (임의로 정하지 않음)**

1. S1의 "5분 이내"와 작업 규칙 6 "제작 시간은 묻지도 표시하지도 않는다"의 충돌.
   앱이 시간을 재서 보여주는 것과 사용자가 검수용으로 한 번 재 보는 것은 다른 행위라 양쪽 다 원문 유지 중.
2. PRD §1.2 표에 **S5가 없음**(S1~S4, S6, S7만 존재). 누락인지 의도인지.
3. NFR-10의 "10단어"가 문장당인지 화면 전체 합산인지. 문장당이면 현재 통과, 합산이면 초과.
4. 오브젝트를 선 위에 그리면 그 칸의 검은 선을 덮어 바닥 센서가 선을 놓칠 수 있음.
   보기에는 위가 맞지만 주행에는 아래가 안전할 수 있음.
5. `말판/` 공식 PDF와 내장 아트의 공개 재배포 조건 문서 유무.

**배포 판단: 아직 아님.** 빌드·경로·치수·글리프는 이번에 직접 재서 전부 깨끗했고 흰 화면 위험도 없습니다.
막는 것은 (1) 데이터가 조용히 사라지는 경로 2건, (2) 종이로 뽑았을 때 드러나는 인쇄 정합 3건,
(3) 공개되면 되돌리기 어려운 권리 문제입니다.

### 2026-09-16 배포 차단 항목 수정 (사용자 지시로 착수)

검토 중 **보고서에 잘못 적었던 것 하나를 정정**했습니다. FR-1.4/1.5를 "코드 존재, 실파일 저장 미검증"으로
분류했는데, 저장소 어댑터 존재만 보고 버튼 배선을 추적하지 않은 탓이었습니다. 실제로는 상단바 저장 버튼이
`notConnectedYet()`에 묶여 토스트만 띄웠고, **편집기에서 맵을 파일로 저장하는 경로가 아예 없었습니다.**
`createMapStore()`는 개발용 `?catalog` 페이지와 능력 탐지용 probe에서만 쓰이고 있었습니다.
그래서 이 항목을 0번으로 올려 가장 먼저 처리했습니다.

또 `docs/02_design-direction.md` §0 전제 2에 "로보메이션 배포 자료 = 저작권 프리, 번들 가능(O7 철회)"가
사용자 결정으로 이미 기록돼 있었습니다. 검토 보고서에서 "근거 문서가 저장소에 없다"고 쓴 것도 정정합니다.
타일 내장은 그 전제대로 유지했고, 전제가 다루지 않는 "원본 PDF 통째 재배포"만 정리했습니다.

**0. 맵 파일 저장 연결 (FR-1.4 / FR-1.5, 둘 다 P0)** — `TopBar.tsx`, `editorStore.ts`, `StartScreen.tsx`

- `editorStore.ts`가 `mapStore`를 export 합니다. 앱 전체가 **인스턴스 하나**를 공유해야 FsaStore가
  파일 핸들을 유지해 "저장"이 대화상자 없이 덮어쓰기됩니다. 기존 `probe`를 이 인스턴스로 대체했습니다.
- 상단바 "저장" → `mapStore.save()`, 아이콘 전용 "다른 이름으로 저장" → `mapStore.saveAs()`.
  단축키 `Ctrl+S` / `Ctrl+Shift+S`. 취소(AbortError)는 토스트 없이 조용히 무시합니다.
  덮어쓰기가 안 되는 브라우저(DownloadStore)에서는 둘이 같은 동작이라 두 번째 버튼을 숨깁니다.
- 저장 성공 시 `saveState='saved'` + 현재 초안 슬롯 정리(이미 파일로 남았으므로).
- 시작 화면 "파일 열기"도 FSA 지원 브라우저에서는 `mapStore.open()`을 씁니다.
  `<input type="file">`로 열면 핸들이 없어서 이후 저장이 늘 "다른 이름으로"가 되기 때문입니다.
  미지원 브라우저는 기존 `<input type="file">`로 그대로 내려갑니다.
- **브라우저 검증**: 가짜 파일 핸들을 주입해 전 과정 확인 — 890바이트 유효 `.hsmap.json` 기록,
  `schema`/`board`/`meta` 정상, 2칸 들여쓰기, 저장 칩 "저장됨" 전환, 초안 슬롯 2→1,
  **두 번째 저장은 대화상자 없이 같은 핸들에 덮어쓰기**(총 2회 기록), 제안 파일명 `새 말판.hsmap.json`.

**1. 초안 슬롯 덮어쓰기** — `src/lib/storage/draft.ts`, `src/App.tsx`

- 슬롯 id 기준을 "페이지 로드 1회당 하나" → "열려 있는 문서마다 하나"로 바꿨습니다.
  `beginDraftSession()`(새 문서) / `adoptDraftSession(id)`(초안 복구 시 원래 슬롯 계승) 추가.
- `flushDraft()` 추가 — 디바운스 대기 중인 마지막 편집을 즉시 반영합니다.
  문서 전환·뒤로가기·탭 닫기에서 부릅니다. 예전에는 전환 시 `clearTimeout`에 걸려 마지막 0.5초가 사라졌습니다.
- `clearDraft()`가 현재 슬롯을 지울 때 보류분도 함께 버립니다(지운 직후 되살아나는 것 방지).
- **브라우저 재현 검증**: 맵 A(타일 1개) → 뒤로가기 → 맵 B(열 6) 전환 후
  수정 전 `slots:1, filled:[0]` (A 소실) → 수정 후 **`slots:2, cols:[6,5], filled:[0,1]`, A 살아있음**.

**2. 탭 닫기·새로고침 이탈 경고 (FR-1.8 / NFR-5, P0)** — `src/App.tsx`

- `beforeunload` 등록. `saveState === 'unsaved'`일 때만 브라우저 기본 경고를 띄웁니다.
  떠나는 김에 `flushDraft()`도 호출해, 사용자가 "나가기"를 골라도 복구할 초안이 남게 했습니다.
- 새 문서를 열 때 `setSaveState('saved')`로 초기화 — 직전 문서의 "저장 안 됨"이 그대로 남아
  손대지도 않은 맵에서 경고가 뜨던 문제(검토 11번)도 같이 해결됐습니다.
- **검증**: 저장 안 된 상태에서 `beforeunload` 디스패치 → `defaultPrevented: true`.

**3. 출발·도착 이름표가 검은 선 위 검은 글씨** — `drawBoard.ts`, `generateMapPdf.ts`

- 검은 글자 뒤에 **흰 후광 1.5mm**를 깝니다. FR-4.2의 "선 위 흰 글씨"를 그대로 쓸 수 없는 이유는
  이름표가 8mm 선보다 가로로 넓어 절반은 흰 종이에 걸치기 때문입니다(흰 글씨면 종이 쪽이 안 보임).
- 캔버스는 `strokeText` → `fillText`, PDF는 텍스트 렌더링 모드 1(Outline)로 한 번 긋고
  모드 0(Fill)로 되돌린 뒤 본 글자를 얹습니다. **래스터화 없이 벡터 유지**.
- 두께 상수는 양쪽 파일에 같은 값(1.5mm)으로 두었습니다. 한쪽만 바꾸면 화면과 인쇄물이 달라집니다.
- **검증**: 화면 확대 스크린샷에서 검은 격자선 위 "출발"이 또렷하게 읽힘.
  PDF 바이트에서 `1 Tr` + `1 1 1 RG` + `4.2519685 w`(=**1.500mm**) 확인, `0 Tr` 복귀도 짝이 맞음.

**4. 타일 7종이 격자선 아래로 깔림** — `manifest.json`, `catalog.ts`, `drawBoard.ts`, `generateMapPdf.ts`

- 판정 기준을 `kind === 'object'` → 타일별 명시 값 `aboveLine`으로 교체했습니다.
  `kind`는 팔레트 묶음·영역 채우기용 모양 분류라 레이어 순서와 1:1로 맞지 않습니다.
- `manifest.json` 35종에 `aboveLine` 추가. **선 위 24종 / 선 아래 11종**.
  `block` 7종 중 수풀·열매 수풀·눈 덮인 바위·눈 쌓인 벽·풀 덮인 바위는 선 위로 올리고,
  **얼음 바닥·발자국은 이름 그대로 바닥이라 선 아래로 남겼습니다**(통째로 올리면 격자선이 끊김).
- 사용자 업로드 이미지는 기존과 같이 선 아래입니다(동작 변경 없음).
- **검증**: 한 맵에 수풀·얼음 바닥·보물상자를 나란히 놓고 스크린샷 —
  수풀·보물상자는 선 위, 얼음 바닥은 선 아래로 정확히 갈림.

**5. 공개 저장소 권리 정리** — `.gitignore`, `src/assets/NOTICE.md`

- `말판/` 공식 PDF 23종(4.6MB)을 `git rm --cached`로 추적 해제하고 `.gitignore`에 추가.
  **파일은 디스크에 그대로 있습니다.** 앱 실행에 쓰이지 않고, 원본 PDF를 그대로 올리는 것은
  전제 2("추출한 타일 번들")가 다루는 범위가 아니기 때문입니다.
- `src/assets/NOTICE.md` 신규 — 타일 35종 출처와 번들 근거(docs/02 §0 전제 2), 아이콘 8종은
  자체 제작, 폰트는 OFL-1.1임을 적었습니다. 로보메이션 배포 조건 문서를 교차 확인하지 못했다는
  점도 사실대로 남겼고, 만약 재배포 불가로 확인되면 되돌릴 경로(docs/01 O7)를 함께 적어 뒀습니다.
- 빈 파일 `rules.md.txt`(0바이트) 삭제 (검토 12번).
- **주의: 추적 해제는 앞으로만 적용됩니다.** PDF 23종은 과거 커밋 `09b8bd0`에 그대로 남아 있습니다.
  히스토리에서 지우려면 `git filter-repo` 같은 재작성 + force push가 필요하고 되돌릴 수 없어서
  임의로 하지 않았습니다. 사용자 지시가 있으면 그때 진행합니다.

**수정 후 재검증 (전부 이번에 직접 실행)**

- `npx tsc -b --force` 오류 0건. `npm run build` 성공(js 1,506.11kB / gzip 629.10kB, css 42.83kB).
  `npm run build:single` 성공(6,758.20kB / gzip 3,403.70kB).
- 브라우저 회귀 스모크: 드롭으로 10x8 맵 열기 → 정답 생성(**23 이동 칸 · 4 회전 · 2 도착점**,
  이름 "도서관"/자동 "도착2") → 정답지 PDF 414ms → A4 4장 지도 PDF 441ms. 콘솔 오류 0건.
- **인쇄 규격이 그대로인지 재확인**: 5쪽 전부 297x210mm, 격자선 폭 **8.0000mm**,
  시트 오프셋 가로 250mm·세로 200mm(셀 경계 정확), 글리프 444개 중 **.notdef 0개**,
  흰 후광 12회 적용.

**남은 것**

- 검토 6~16번(배포 후 수정)과 17~28번(사용자 실기)은 그대로 남아 있습니다.
- 특히 **6번(50mm 눈금자·100% 인쇄 안내가 프린터 인쇄 불가 여백에 위치)** 은 S2 자 실측을
  직접 방해하므로, 인쇄 검증 전에 먼저 처리하는 편이 낫습니다.
- 검토 블록의 질문 1~5는 아직 사용자 확인 대기입니다.

### 2026-09-16 검토 6번 수정 — 인쇄 안내 요소가 프린터 인쇄 불가 여백에 있던 문제

**무엇이 문제였나.** 50mm 검증 눈금자가 종이 아래 끝에서 1.4mm 지점(눈금 0.7~2.7mm, "50 mm" 글자 0.65mm),
"※ 실제 크기(100%)로 인쇄하세요" 안내가 위 끝에서 3.5mm 지점에 있었습니다. 학교에서 흔히 쓰는
레이저 복합기·잉크젯은 A4 상하 4.2~5mm를 아예 인쇄하지 못하므로 **둘 다 종이에 안 찍혔습니다.**
자로 재라고 넣은 눈금자가 정작 인쇄물에 없어서 S2(피치 50mm ±0.5mm 실측)를 할 수단이 없었습니다.
FR-6.5·FR-6.6이 "전 시트"라고 못 박은 P0 항목인데 실물에서 빠져 있던 셈입니다.

**어떻게 고쳤나** — `src/lib/pdf/generateMapPdf.ts`

- `SAFE_PRINT_EDGE_MM = 8` 도입. 위 기종들의 인쇄 불가 폭(4.2~5mm)을 모두 넘기는 값입니다.
- 맵은 항상 용지 한가운데 놓이므로(`paperLayout` / `tiledLayout`) 좌우 여백 = `offsetXmm`,
  상하 여백 = `offsetYmm` 입니다. 칸이 50mm이고 용지 규격은 50의 배수가 아니라서
  **두 축 중 한쪽에는 반드시 큰 자투리가 남습니다.** 그 넓은 쪽 띠에 안내 요소를 넣으면
  맵을 조금도 가리지 않으면서 인쇄 가능 영역 안에 들어갑니다.
  - A4 가로 5×4 → 좌우 23.5mm / 상하 5mm → **세로 배치**(눈금자 왼쪽 띠, 안내 문구 오른쪽 띠에 90° 회전)
  - A4 세로 4×5 → 좌우 5mm / 상하 23.5mm → **가로 배치**(기존과 같은 모양, 위치만 안쪽으로)
- 판정은 `guidePlacement(layout)` 한 곳에서 합니다(`GUIDE_MIN_BAND_MM = 14`).
  두 축 모두 좁은 드문 경우에만 가로로 두되 흰 바탕을 깔아 맵 위에 겹칩니다.
- 가로 배치일 때 눈금자 시작 x는 `max(offsetXmm, SAFE_PRINT_EDGE_MM)`로 당깁니다 —
  맵 왼쪽 끝에 맞추는 게 원래 의도지만, 그 위치가 인쇄 불가 영역이면 소용이 없기 때문입니다.
- 회전은 `pdf-lib`의 `degrees(90)`을 씁니다. 이때 글자는 위로 진행하고 글자 높이는 x가 작아지는
  쪽으로 자라므로, 기준 x를 띠의 오른쪽 끝에 두면 글자가 통째로 띠 안에 들어옵니다.

**건드리지 않은 것.** 크롭 마크·재단 점선·시트 번호·이웃 방향 표시는 그대로입니다.
이것들은 맵 영역(재단선)을 기준으로 위치가 정해져야 의미가 있고, 시트 번호·이웃 표시는
이미 맵 영역 안쪽에 흰 바탕을 깔고 그려서 인쇄 가능 영역 안에 있습니다.
다만 **재단 마크는 재단선 바깥으로 3mm 뻗어 나가므로, A4 가로처럼 상하 여백이 5mm인 시트에서는
종이 끝에서 2mm 지점에 찍혀 잘려 나갈 수 있습니다.** 재단 점선 자체(여백 5mm 지점)는 살아남습니다.
마크를 안쪽으로 옮기면 "재단선을 가리킨다"는 의미가 달라지므로 임의로 바꾸지 않았습니다 — 사용자 판단 대기.

**검증 (이번에 직접 실행)**

생성된 PDF 바이트를 풀어 눈금자 선분 좌표를 직접 쟀습니다.

| 경우 | 배치 | 눈금자 실측 길이 | 종이 끝까지 최소 거리 |
|---|---|---|---|
| A4 가로 5×4 단일장 | 세로 (x=9.5mm, y 80→130) | **50.000mm** | **8mm** (기존 0.65mm) |
| A4 세로 4×5 단일장 | 가로 (x 8→58, y=9.5mm) | **50.000mm** | **8mm** |
| A4 가로 10×8 → 4장 분할, 시트 1~4 | 세로 (x=9.5mm, y 80→130) | **50.000mm** | **8mm** |
| 같은 PDF의 조립 안내도 | 가로 (x 16→66, y=9.5mm) | **50.000mm** | **8mm** |

- 시트 4장 모두 회전 텍스트 3개 확인: 안내 문구(20글리프, x=289 = 297−8), 시트 번호(12글리프, x=289, y=8),
  "50 mm"(5글리프, x=13.4). 맵 영역은 x 23.5~273.5이므로 **어느 것도 맵을 가리지 않습니다.**
- 조립 안내도의 기존 안내 문장(y=14mm)과 새 눈금자(y 8~11mm)는 겹치지 않습니다.
- `npx tsc -b --force` 오류 0건, `npm run build`(js 1,506.93kB / gzip 629.35kB)·`npm run build:single` 성공.

**후속: 재단 마크도 여백에 맞춰 적응하게 수정** — `src/lib/pdf/generateMapPdf.ts`

사용자 요청으로 6번의 파생 항목(재단 마크가 잘리는 문제)까지 처리했습니다.
단순히 길이만 절반으로 줄이면 A4 가로에서는 여전히 3.5mm 지점이라 인쇄 불가 영역을
못 벗어나고, 반대로 여백이 넉넉한 변까지 멀쩡한 마크가 짧아집니다. 그래서 **변마다 따로**
판단하게 했습니다(`markReach` / `markOffset` / `triangleHeight`).

- 바깥 여백이 `MARK_SAFE_EDGE_MM(6)`보다 1.2mm 이상 남으면 → 기존대로 바깥 3mm
- 남지 않으면 → **안쪽으로 뒤집어 1.5mm**. 꼭짓점은 여전히 재단선을 정확히 가리키므로
  가위를 맞추는 데 지장이 없고, 격자선은 재단선에서 21mm 안쪽부터 시작하므로(칸 중심 25mm −
  선폭 절반 4mm) 선을 건드리지 않습니다. 모서리 칸에 아트 타일이 있으면 그림 위에 겹쳐 보입니다.
- 변 중앙 정렬 삼각 마크(FR-6.4)도 같은 규칙. 밑변 절반(2.2mm)은 고정하고 높이만 조정·반전합니다.
- 네 변의 여백을 각각 잽니다 — 겹치기(overlap) 이음매를 쓰면 맵이 한쪽으로만 넓어져
  좌우·상하 여백이 서로 달라지기 때문입니다.

**검증 (PDF 바이트 실측)**

| 용지 | 상하 방향 마크 | 좌우 방향 마크 | 종이 끝까지 최소 |
|---|---|---|---|
| A4 가로 10×8 → 4장 (상하 여백 5mm) | **안쪽 1.5mm** (y 5→6.5, 203.5→205) | 바깥 3mm (x 20.5→23.5) | 상하 **5mm** (기존 2mm) / 좌우 20.5mm |
| A4 세로 8×10 → 4장 (좌우 여백 5mm) | 바깥 3mm (y 20.5~276.5) | **안쪽 1.5mm** (x 5~205) | 상하 20.5mm / 좌우 **5mm** (기존 2mm) |

삼각 마크도 같이 반전 확인(A4 가로: 위쪽 밑변 y=203.5·꼭짓점 y=205, 아래쪽 밑변 y=6.5·꼭짓점 y=5).

**남은 한계 — 사용자 확인 필요.** 마크가 닿는 재단선 자체가 A4 가로 5×4에서 종이 끝 5mm 지점입니다
(맵 200mm + 상하 여백 5mm씩 = 210mm). 맵 위치는 바꿀 수 없으므로 **5mm가 물리적 하한**입니다.
인쇄 불가 폭이 4.2mm인 기종에서는 나오고, 5mm인 기종에서는 아슬아슬합니다.
실제 프린터에서 재단선과 마크가 찍히는지는 인쇄해 봐야 알 수 있습니다(S3 실기 항목에 포함).

### 2026-09-16 검토 7번 — 경계 진입로 추가·제거 (FR-2.4) 구현

**분석.** `Stub` 타입·화면 렌더·PDF 렌더·직렬화·격자 크기 변경 처리는 전부 이미 있었고
프리셋 2종(배리어프리·재난구조)이 실제로 쓰고 있었습니다. **빠진 건 추가·제거 수단 하나뿐**이었습니다.

**설계 — 새 도구를 만들지 않고 `L`(격자선 긋기)에 얹음**

진입로는 결국 "경계 노드에서 바깥으로 뻗는 반 칸(25mm)짜리 격자선"이라 L과 같은 종류입니다.
도구 레일이 이미 11개라 12번째를 늘리면 첫 화면이 그만큼 복잡해집니다(NFR-10).

- **격자 바깥을 클릭** → 가장 가까운 경계 노드의 그 방향 진입로 토글
- 인식 범위는 격자 경계에서 바깥으로 **반 칸(25mm)**까지. 그게 곧 진입로가 그려지는 길이라
  "보이는 선 위를 클릭하면 반응한다"가 자연스럽게 성립합니다
- 모서리 바깥을 클릭하면 **더 많이 벗어난 쪽** 변이 이깁니다
- **Alt+클릭 = 제거** (Alt+드래그가 이미 지우기라 같은 규칙)
- 팔레트 L 패널에 일괄 버튼 2개 — 공식 playbot 말판처럼 경계 전체에 넣으려면
  A4 5x4도 18번, A0 23x16이면 78번 클릭해야 해서 필요합니다. 현재 개수도 함께 표시합니다

**바뀐 파일**

- `src/features/canvas/gridMath.ts`: `stubAtPoint` `stubExists` `setStub` `toggleStub`
  `allBoundaryStubs` `isInsideBoard` 추가
- `src/features/canvas/toolInteractions.ts`: L 도구 단순 클릭 분기
- `src/features/palette/PalettePanel.tsx` + `.module.css`: L 전용 패널과 일괄 버튼

**구현 중 발견해 같이 고친 것 — 빈 곳 클릭이 격자선을 지우던 문제**

`nearestEdgeToPoint`는 범위 검사를 하지 않아서, 종이에서 한참 떨어진 빈 곳을 클릭해도
"가장 가까운" 엣지를 찾아 토글했습니다. 팬을 하려다 살짝 클릭만 되어도 엉뚱한 격자선이
사라집니다. FR-2.2는 "엣지 단건 클릭 토글"이지 "아무 데나 클릭해도 토글"이 아니므로,
`isInsideBoard`로 격자 안쪽일 때만 엣지를 토글하게 막았습니다.
(이번 검증 중 실제로 이 경로로 엣지 2개가 지워지는 걸 확인하고 추가했습니다.)

**검증 (이번에 직접 실행)**

- `stubAtPoint` 순수 함수 12케이스 전부 통과 — 격자 안쪽 null, 네 방향 경계 노드 선택,
  경계 바로 밖 0.5mm 인식, 반 칸 경계 25.0mm 인식, 25.1mm 초과 null,
  모서리 대각 바깥에서 더 많이 벗어난 변 선택(2케이스)
- 실제 클릭: 격자 위쪽 바깥 3곳 클릭 → `(0,0)N` `(2,0)N` `(4,0)N` 생성,
  **격자 엣지는 16/15 그대로**(빈 곳 클릭 방지가 동작). 같은 자리 재클릭으로 제거,
  Alt+클릭으로 제거 확인
- 일괄 버튼: 5x4에서 "경계 전체에 넣기" → **18개**(= (5+4)x2, 계산과 일치),
  "모두 빼기" → 0개, 실행취소로 18개 복원
- 화면: 격자가 종이 네 변 끝까지 닿는 공식 playbot 말판 모양으로 렌더됨
- **PDF 실측**: 선분 49개 중 **25.000mm가 18개(진입로)**, **50.000mm가 31개(격자 엣지 16+15)**
- `npx tsc -b --force` 0건, `npm run build`(js 1,509.69kB / gzip 630.13kB)·`npm run build:single` 성공

**손대지 않은 것 (사용자 판단 필요)**

- `createFullGridMap`의 `stubs` 기본값은 여전히 `[]`입니다. 이제 지울 수단이 생겼으니
  기본으로 넣을 수 있지만, 넣으면 **프리셋 8종과 새 맵의 모양이 전부 바뀝니다.**
  공식 playbot 말판과 같아지는 대신, 진입로를 원치 않는 사용자는 매번 빼야 합니다.
- `resize.ts`는 격자 크기를 바꿀 때 범위를 벗어난 진입로만 지웁니다. 격자를 키우면
  기존 경계 진입로가 **안쪽 노드의 반 칸짜리 돌기**로 남습니다. 자동으로 지우면
  프리셋의 의도적인 안쪽 돌기(재난구조의 `{node:[5,6],dir:'E'}` 같은 것)까지 지워질 수 있어
  임의로 바꾸지 않았습니다.

**후속: 기본값과 크기 변경 처리 (2026-09-16 사용자 결정)**

7번 구현 뒤 남겨 뒀던 두 질문에 사용자가 답해 확정했습니다.

**결정 1 — 새 맵·프리셋에 진입로를 기본으로 넣는다.** `createFullGridMap`이 이제
`allBoundaryStubs(cols, rows)`를 함께 돌려줍니다. 근거: 로보메이션 공식 playbot 말판이 그 모양이고,
교과서 말판 재현이 이 앱의 주 용도입니다. 예전에 비워 둔 이유("넣으면 지울 방법이 없다")는
FR-2.4 편집 수단이 생겨 사라졌습니다. 필요 없으면 팔레트에서 "모두 빼기" 한 번입니다.
격자선이 하나도 없는 `createEmptyMap`("빈 격자" 프리셋)에는 넣지 않습니다 — 이어질 선이 없는데
가장자리에만 토막이 떠 있으면 뜻을 알 수 없기 때문입니다.

**결정 2 — 크기를 바꾸면 가장자리를 벗어난 진입로를 자동 정리한다.** `resize.ts`의 `resizeStubs`.
- 사방이 전부 채워져 있던 맵(= 새 맵 기본값)이면 새 크기에 맞춰 **다시 사방을 채웁니다.**
  이게 없으면 크기를 줄였을 때 새로 생긴 가장자리 두 면에만 진입로가 없어 반쪽짜리가 됩니다.
- 손으로 골라 넣은 맵이면 임의로 늘리지 않고 정리만 합니다. 격자 밖으로 나간 것과
  "예전에는 가장자리였는데 지금은 아니게 된 것"만 버리고, **원래부터 안쪽에 있던 돌기는 보존**합니다
  (재난구조 프리셋처럼 일부러 넣어 둔 것을 크기 변경을 이유로 지우지 않기 위해).

**구조 정리.** 순수 데이터 조작(`stubExists` `setStub` `toggleStub` `isBoundaryStub`
`allBoundaryStubs` `isFullBoundarySet`)을 `src/lib/model/stubs.ts`로 옮겼습니다.
`factory.ts`와 `resize.ts`가 써야 하는데 `lib/model`이 `features/`를 가져다 쓰면 의존 방향이
거꾸로 되기 때문입니다. 화면 좌표를 다루는 `stubAtPoint`만 `features/canvas/gridMath.ts`에 남겼습니다.

**검증 (이번에 직접 실행)**

| 경우 | 결과 | 기대 |
|---|---|---|
| 새 맵 5x4 | 18개, 안쪽 돌기 0 | 18 / 0 |
| 빈 격자 | 0개 | 0 |
| 라인트레이서(무격자) | 0개 | 0 |
| 키우기 5x4 -> 7x5 | 24개, 안쪽 돌기 0 | (7+5)x2 = 24 / 0 |
| 줄이기 5x4 -> 3x3 | 12개, 안쪽 돌기 0 | (3+3)x2 = 12 / 0 |
| 손으로 고른 2개 -> 7x5 | 2개 그대로(`0,0W` `2,2E`) | 임의로 늘리지 않음, 안쪽 돌기 보존 |

- 프리셋 8종 진입로 개수: a4-basic 18 / a4-two 28 / a4-four 36 / a2-one 38 / empty 0 /
  배리어프리 12(자체 지정) / 재난구조 20(자체 지정) / 라인트레이서 0 — 전부 `(cols+rows)x2` 또는 의도값과 일치
- 화면: A4 4장 프리셋에서 격자가 종이 네 변 끝까지 닿는 것 확인
- `npx tsc -b --force` 0건, `npm run build`(js 1,510.06kB / gzip 630.26kB)·`npm run build:single` 성공

**알아 둘 점.** 진입로가 기본으로 들어가면서 인쇄 시 잉크가 종이 끝에서 5mm 지점까지 갑니다
(A4 가로 기준. 맵 200mm + 상하 여백 5mm씩). 인쇄 불가 폭이 5mm인 프린터에서는 진입로 끝
몇 mm가 잘릴 수 있습니다. 격자선 본체와 칸 간격에는 영향이 없습니다.

### 2026-09-16 배포 전 마무리 — 검토 10·14·9·13번

**10. 개발용 카탈로그를 배포본에서 제거** — `EditorLayout.tsx`, `App.tsx`

- 캔버스 구석의 "컴포넌트 카탈로그 (개발용)" 링크를 `import.meta.env.DEV`로 감쌌습니다.
- `?catalog` 라우트도 배포본에서는 열리지 않습니다(`isCatalogRoute`가 DEV가 아니면 false).
- 링크만 숨기면 **화면 코드는 그대로 번들에 실립니다.** 아무도 열 수 없는 화면 때문에 첫 로딩
  용량이 늘어나므로 `lazy(() => import(...))`로 떼어냈습니다.
  메인 번들 1,509.89kB → **1,494.25kB**(gzip 630.20 → 627.19), 카탈로그는 별도 17.38kB 파일로 분리.
  배포 번들에서 "컴포넌트 카탈로그" 문자열 **0건** 확인.

**14. 파비콘(탭 아이콘) 추가** — `index.html`

말판의 실제 모습(굵은 검은 격자선)을 단순화한 SVG를 **data URI로 인라인**했습니다.
`public/`에 파일로 두면 USB로 들고 다니는 단일 HTML 대피로 빌드에서 파일이 따로 떨어져 나가
"한 파일" 약속이 깨집니다. 인라인하면 일반 빌드·대피로 빌드 양쪽에서 추가 요청 없이 동작합니다.

**9. robots.txt — 검토 지적이 틀렸음. 파일을 삭제하는 것으로 정리**

검토 보고서에 "`Allow: /`가 `noindex` 메타와 방향이 반대"라고 썼는데 **틀렸습니다.**
크롤러가 페이지를 읽어야 `noindex` 표시를 볼 수 있으므로 둘은 같이 써야 맞는 조합입니다.
접근을 막으면 오히려 표시를 못 보고, 다른 곳에 링크가 걸리면 주소만 검색 결과에 남을 수 있습니다.

진짜 문제는 따로 있었습니다. **robots.txt는 도메인 최상위에서만 읽힙니다.**
이 앱은 `t777-kaewoong.github.io/hamsterCreator/` 하위 경로라 거기에 robots.txt를 둬도
크롤러가 아예 읽지 않습니다(최상위 robots.txt는 사용자 루트 Pages 저장소의 것입니다).
그래서 `robots.txt`를 삭제하고, 실제로 동작하는 `noindex` 메타만 남긴 뒤 이유를 주석으로 적었습니다.

**13. 배포 워크플로 `cancel-in-progress: false`** — `.github/workflows/deploy.yml`

true면 연달아 푸시했을 때 앞 배포가 **중간에** 끊겨 사이트가 반쯤 올라간 상태로 남을 수 있습니다.
GitHub 공식 Pages 예시도 false를 권합니다. 어차피 마지막 것이 최종 결과가 됩니다.

**추가로 발견해 고친 것 — `npm run preview`가 배포본을 못 열던 문제** (`vite.config.ts`)

vite는 `vite preview`에도 `command: 'serve'`를 넘깁니다. 설정이 command만 보고 base를 정해서
preview는 base를 `/`로 잡는데, dist/index.html 안의 자산 주소는 `/hamsterCreator/...`로 구워져
있어 **전부 404 나고 흰 화면**만 나왔습니다. 배포 직전 확인용 명령이 정작 배포본을 못 여는
상태였습니다. `isPreview`를 함께 보도록 분기를 나눴습니다.
→ 확인은 `npm run preview` 후 **http://localhost:4173/hamsterCreator/** 로 엽니다.

**검증 (이번에 직접 실행)**

- 배포 빌드를 실제 `/hamsterCreator/` 경로에서 열어 확인: 시작 화면 정상 렌더,
  프리셋 8장 썸네일 표시(격자가 종이 끝까지 닿는 새 기본값 반영됨), 자산 전부 200,
  **카탈로그 링크 없음**, `noindex` 메타·파비콘 태그 존재
- `npx tsc -b --force` 0건, `npm run build`·`npm run build:single` 성공
- 남는 사소한 것: 브라우저가 `/favicon.ico`를 관례적으로 한 번 더 찾아 404가 납니다.
  SVG 파비콘을 선언해도 일부 브라우저는 .ico를 확인합니다. 동작에는 영향 없습니다.

## Decisions
- 2026-09-05 분석·설계·PRD를 먼저 작성하고 구현 착수 (`docs/01`~`03`). 코드부터 쓰지 않음
- 2026-09-05 DB 없이 `.hsmap.json` 파일 저장/불러오기로 운영. 사용자 업로드 이미지는 파일에 base64로 내장해 자기 완결적으로 만듦
- 2026-09-05 주 배포는 GitHub Pages 정적 호스팅. Apps Script는 선택지로만 보류 (샌드박스 iframe이 덮어쓰기 저장을 막음)
- 2026-09-05 기본 맵 크기 = A4 가로 1장 (5×4 = 20칸, 250×200mm)
- 2026-09-16 새 맵·격자 프리셋에 **경계 진입로를 기본 포함**. 공식 playbot 말판과 같은 모양(사용자 결정)
- 2026-09-16 격자 크기 변경 시 가장자리를 벗어난 진입로는 자동 정리, 사방 세트였으면 새 크기로 재생성(사용자 결정)
- 2026-09-05 자유곡선 트랙을 v1에 포함. PDF 렌더러를 두 번 손대지 않도록 M1.5(격자 다음, PDF 앞)에 배치
- 2026-09-05 라이트 모드만 구현. 캔버스가 실제 흰 종이를 표현하므로 다크 모드는 종이 색 인지를 방해함
- 2026-09-05 스타일은 CSS 커스텀 프로퍼티 + CSS Modules. 유틸리티 프레임워크 미사용 (토큰을 단일 진실 공급원으로)
- 2026-09-05 **작업 방식**: 코딩은 Sonnet, 모호하거나 추상적인 판단은 Opus로 올려서 처리. 단계 종료마다 이 문서에 기록 + 한 줄 보고. 코드 주석은 사용자가 직접 수정할 수 있도록 한글로 구체적으로 작성

## Completed
- [x] **M0-1 프로젝트 스캐폴딩 + 배포 파이프라인** (2026-09-05)
  - Vite 8.2.2 + React 18.3.1 + TypeScript 7.0.2, `npm run build` 성공 (140.9kB, gzip 45.9kB)
  - `base: '/hamsterCreator/'` 적용 확인. 대피로 빌드 `npm run build:single`도 성공 (단일 HTML 141.3kB)
  - GitHub Actions Pages 워크플로 작성 (`main` push + 수동 실행)
  - git 초기화, 브랜치 `main`, 첫 커밋 `09b8bd0`
  - 우회 사항: npm이 React 19를 기본 설치 → 18로 고정 / TS 7이 `baseUrl` 제거 → `paths`를 상대경로로 / 윈도우 셸 호환 위해 `cross-env` 추가

- [x] **M0-2 디자인 토큰 + 전역 스타일** (2026-09-05)
  - `tokens.css` — PRD §9.3~9.5의 토큰 49개 전수 반영 (중립/강조/의미색/캔버스/반경/그림자/모션/간격)
  - `reset.css` — box-sizing, `:focus-visible` 링(포커스 링 제거 안 함), `prefers-reduced-motion` 대응
  - `typography.css` — 타이포 스케일 7종 클래스 + `.t-nums`
  - `App.tsx`를 토큰 확인용 임시 화면으로 교체. 하드코딩 hex 0건, 전부 토큰 참조
  - 커밋 `bc9ca79`
- [x] **M0-2a 폰트 전략 수정** (2026-09-05) — Opus 판단으로 교체
  - 문제: `pretendard-dynamic-subset`이 폰트 1,647개 / 22.3MB, CSS 548KB 생성 → 대피로 단일 HTML 빌드가 31MB로 사용 불가
  - 해결: PRD §9.4가 쓰는 400/500/600 굵기만 고정 서브셋 woff2 3개로 직접 `@font-face`. `pretendard` 패키지 제거, 파일을 `src/assets/fonts/`로 이관
  - 결과: 폰트 **804KB/3개**, CSS **5.32KB**, 대피로 빌드 **1.23MB**, dist 전체 949KB/7파일
  - OFL-1.1 라이선스 고지 추가 (`src/assets/fonts/LICENSE.txt`) — 저장소가 Public이라 필요
  - 커밋 `650d42b`
  - 브라우저 실측 검증: 토큰 49개 전부 정상 렌더, `font-family`가 Pretendard로 해석됨

- [x] **M0-3 기본 컴포넌트** (2026-09-05)
  - `src/components/`에 8종 추가: `Button` `Input` `Segmented` `TabPills` `Tooltip` `StatusChip` `Toast`(+`useToast`) `Modal`, `index.ts`로 재export
  - PRD §9.7 수치 그대로 구현. `App.tsx`를 컴포넌트 카탈로그로 확장(기존 토큰 확인 섹션은 유지)
  - 인터랙션 전부 브라우저에서 실측 검증: 숫자 입력 화살표(1/Shift+10), Segmented 좌우 화살표 이동,
    Modal 포커스 트랩(Tab 순환)·Esc·body 스크롤 잠금·포커스 복귀, Toast 교체(1개만)·3초/6초 자동 소멸,
    Tooltip 400ms 지연 표시·즉시 사라짐·단축키 칩, StatusChip saving 점 깜빡임, TabPills 스크롤 페이드 좌우 전환
  - 하드코딩 hex 리터럴 0건(`grep -rE "#[0-9A-Fa-f]{6}" src/components/` 확인)
  - 토큰 공백 발견: 역상(흰) 글자용 토큰이 tokens.css에 없음 — PRD가 "#FFF"로 직접 리터럴 지정한 자리
    (primary/danger 버튼 글자, Tooltip/Toast 글자)에 한해 CSS 키워드 `white`를 그대로 씀. `--c-text-inverse`
    같은 토큰 추가를 다음 토큰 개정 때 권장
  - 번들: JS 147.14kB→164.78kB(gzip 47.62kB→53.15kB), CSS 5.33kB→13.97kB(gzip 1.82kB→3.79kB, 아이콘 6개 트리셰이킹 포함)
  - 커밋 `a1eb896`
- [x] **M0-3a 역상 색 토큰 보강** (2026-09-05) — Opus 판단
  - M0-3에서 발견된 토큰 공백(진한 배경 위 글자색이 토큰에 없음)을 메움
  - `--c-text-inverse` `--c-surface-inverse` `--c-overlay` 3종 추가, Button/Toast/Tooltip/Modal의 리터럴 6곳 치환
  - `docs/03_prd.md` §9.3에도 동일 토큰 반영해 명세와 코드를 일치시킴
  - 커밋 `35c30e7`

- [x] **M0-4 맵 문서 타입 + 파일 저장소 어댑터** (2026-09-05) — **M0 완료**
  - `src/lib/model/` — `types.ts`(PRD §5 스키마, Stroke는 kind 판별 유니온) `constants.ts`(50mm·8mm·용지 8종) `factory.ts`(기본 A4 5×4) `serialize.ts`(예외 대신 결과 객체 반환)
  - `src/lib/storage/` — `FsaStore`(파일 핸들 유지 → 진짜 덮어쓰기) `DownloadStore`(폴백) `draft.ts`(초안 5개·디바운스 500ms·userAssets 제외) `index.ts`(`createMapStore()` 자동 선택)
  - `App.tsx`에 저장소 확인 섹션 추가 (기존 토큰·컴포넌트 섹션 유지)
  - 번들: JS 174.84kB(gzip 56.41kB), CSS 14.24kB(gzip 3.85kB), 대피로 1.26MB
  - 커밋 `22315b1`
  - **Opus 추가 검증**(에이전트가 파일 대화상자 때문에 못 한 부분을 모의 핸들로 대체해 실측):
    `saveAs` → 대화상자 1회 호출·2040바이트 기록·writable close 확인 /
    `save` → **대화상자 재호출 없이 같은 핸들에 덮어쓰기 확인**(핸들 유지가 실제로 동작) /
    `open` 왕복 결과가 원본과 완전 동일 /
    취소(DOMException AbortError) → `UserCancelledError` 변환 확인, 진짜 오류(TypeError)는 취소로 오인하지 않음 /
    초안 왕복 + `userAssets`가 실제로 제외됨 확인
  - **남은 미검증**: OS 네이티브 파일 대화상자를 실제로 열어 디스크에 쓰는 경로(자동화 불가, 사람이 한 번 눌러봐야 함)

- [x] **M1-0 아트 타일 추출** (2026-09-05) — Opus 직접 수행
  - `말판/board_game_pdf_ko/custom_objects.pdf`에서 고유 타일 **35종** 추출 (앞서 36종이라 한 건 오산 — 실제 고유 이미지는 35개)
  - **투명도 복원**: PDF는 알파를 별도 SMask로 보관해서 그냥 뽑으면 배경이 검게 나옴. SMask를 합성해 35종 전부 알파 살림
  - **용량 최적화**: 2,468KB → **438KB** (82% 감소). 팔레트 양자화/최적화 중 파일별로 더 작은 쪽 채택. 육안 확인 결과 색 손상 없음
  - 원본 해상도 433×433px 유지 (50mm 기준 약 220dpi) — PDF 내보내기에서 재압축 금지(FR-6.10) 때문
  - 대조표를 눈으로 보고 한글 이름 부여. 테마 6종: 던전6 숲4 얼음6 모험7 사탕5 공룡7
  - 분류 신설: `floor` 15(이어붙는 바닥) / `block` 8(칸 안 블록) / `object` 12(낱개 물건).
    변(edge) 중앙의 불투명도로 판정 — 전체 불투명 비율만 보면 모서리 라운딩 때문에 오판함
  - `src/lib/tiles/catalog.ts` + `src/assets/tiles/manifest.json` 추가. `tsconfig.app.json`에 `resolveJsonModule` 활성화
  - 브라우저 검증: 35종 로드, 테마별 개수 일치, 이미지 433×433 실제 로드 확인
  - PRD FR-8.1·§3.1의 "36종"을 35종으로 정정

- [x] **M1-1 레이아웃 골격 + 도구 레일** (2026-09-05)
  - `src/features/editor/` — `EditorLayout` `TopBar` `ToolRail` `editorStore`(zustand)
  - 기존 카탈로그 화면은 `src/features/catalog/CatalogPage.tsx`로 이동. `?catalog`로 접근(개발용)
  - **브라우저 실측 검증** (뷰포트 1320×860): 상단바 56 / 도구레일 64 / 팔레트 280 / 인스펙터 300 / 캔버스 676, 합계 1320 — PRD §9.2 명세와 전부 일치
  - 도구 버튼 44×44px, 11종(V L B R I E T M / P D O), 그룹 구분선 확인
  - **단축키 11종 전부 동작 확인**. 입력창 포커스 중에는 차단되는 것도 확인
  - 뷰포트 1150px(<1180)에서 인스펙터 자동 접힘 확인 (§9.2 명세)
  - 하드코딩 색 0건. 번들 JS 188.28kB(gzip 61.10kB), CSS 18.27kB, 대피로 1.28MB
  - 커밋 `6b0e95a`
  - 알아둘 점: 좁은 창에서 자동 접힌 뒤 창을 다시 넓혀도 인스펙터가 자동으로 펴지지는 않음(수동 버튼 필요). PRD가 접기만 규정해서 그대로 뒀으나, 사용성 다듬을 때 재검토 대상

- [x] **M1-2 캔버스 뷰포트** (2026-09-05)
  - `src/features/canvas/` — `viewport.ts`(순수 좌표 변환 클래스) `renderer.ts`(DPR 대응 5레이어 + 더티 플래그 + rAF 배칭) `drawBoard.ts` `ruler.ts` `minimap.ts` `cssTokens.ts` `tileBitmaps.ts` `CanvasViewport.tsx`
  - 레이어 순서 `paper → art → grid → props → overlay` (§5 렌더 순서와 일치). 오버레이는 다음 단계용으로 비워둠
  - 에이전트 실측: DPR 정확(캔버스 width = CSS폭 × dpr) / 맞춤 여백 48px 정확 / **커서 기준 확대 시 커서 아래 픽셀 불변 확인** / 배율 클램프 0.5~8 정확 / 눈금자 50mm 간격 오차 0.4px 이내 / 안내선 `pxPerMm<1.2` 숨김 동작 / 격자선 굵기가 배율 따라 변함
  - 번들: JS 232.57kB(gzip 81.88kB), CSS 20.00kB. 대피로 1.90MB(타일 35종이 data URI로 인라인되어 증가 — 정상)
  - 커밋 `e0c7195`
- [x] **M1-2a 격자선 끝 처리 수정** (2026-09-05) — Opus 판단
  - 결함: `lineCap: 'round'`라 선이 끝점보다 **4mm(선폭의 절반) 길어짐**. 화면엔 티가 안 나지만 인쇄 실측 치수가 어긋나고, 바깥 모서리가 둥글게 나와 공식 말판과 다름
  - 수정: `lineCap: 'butt'`. 다만 butt으로만 두면 선이 꺾이는 자리에 선폭 절반짜리 정사각형 빈틈이 생기므로, **선이 2개 이상 만나는 노드에만** 선폭 크기 사각형을 채움. 막다른 길(선 1개) 노드는 채우지 않아 길이가 늘지 않음
  - 브라우저 확인: 모서리 각짐, 막다른 길 끝이 연장 없이 끊김
  - 커밋 `228f8b0`
  - 오해였던 것: 스크린샷만 보고 "아트가 격자선 위에 그려진다"고 의심했으나, 코드상 `LAYER_ORDER`가 art→grid라 선이 위에 그려지는 게 맞음. 결함 아님

- [x] **M1-3 팔레트 패널 + 인쇄용 아이콘** (2026-09-05)
  - `src/features/palette/PalettePanel.tsx` — 검색 / 테마 탭 9개 / **77px 3열 타일 그리드**(PRD §9.11 수치 그대로)
  - 타일 클릭 → `stampTileId` 설정 + **도구가 자동으로 타일(B)로 전환** (PRD: 사용자가 도구를 먼저 고르게 강요하지 않음)
  - 검색은 테마를 가로질러 동작. 타일 아래 이름 없이 툴팁으로만(PRD U2: 글자 아닌 그림이 채움)
  - `src/assets/icons/` — **인쇄용 SVG 8종** 신설(출발 깃발·도착 깃발·불·밸브·사람·신호등·햄스터·방향 화살표). 얇은 선 대신 꽉 찬 면으로 그려 50mm 인쇄에서도 형태 유지
  - 내 이미지 업로드 → 정사각 중앙 크롭 + 긴 변 433px 축소 + base64로 `doc.userAssets`에 내장(PRD D4)
  - `Input`에 선택적 `icon` prop 추가(검색창용, 기존 호출부 영향 없음)
  - 에이전트 실측: 타일 77×77px, 패널 280px, 클릭 시 도구 전환, 검색 교차 동작, 업로드 크롭 정확
  - 브라우저 육안 확인: 팔레트가 그림으로 가득 참, 아이콘 8종 형태 양호, 좁은 화면(<1024px) 안내 배너 정상
  - 번들: JS 243.61kB(gzip 85.34kB), CSS 21.90kB. 대피로 1.91MB
  - 커밋 `886ca00`, `f9cf494`
- [x] **M1-3a 아이콘 누락 보완 + 명세 정정** (2026-09-05) — Opus 판단
  - PRD §9.6이 요구한 8종 중 **방향 화살표가 빠져 있어** 추가
  - PRD의 아이콘 viewBox 규격을 100×100 → **50×50**으로 정정. 타일이 50mm이므로 1단위=1mm가 되어 코드에서 인쇄 크기를 바로 가늠할 수 있음
  - 커밋 `f9cf494`

- [x] **M1-4 도구 동작 + 캔버스 오버레이** (2026-09-05)
  - `src/features/canvas/` — `gridMath.ts`(셀·노드·엣지 기하) `toolInteractions.ts`(`ToolController`) `drawOverlay.ts`(오버레이 레이어)
  - 도구: B 타일 스탬프 / L 격자선 긋기(+Alt 지우기) / R 영역 채우기(+Alt 같은 테마 랜덤) / I 스포이드(집으면 B로 자동 전환) / E 지우개 / V 선택(뼈대만)
  - 우클릭 재배치, R 회전·F 반전, 오버레이 4종(호버·스탬프 고스트 0.55+2px 파선·선 긋기 미리보기·영역 사각형+크기 칩)
  - **실행취소: 스냅샷 + 제스처 묶기.** 드래그 한 번이 `Ctrl+Z` 한 번에 전부 되돌아감. 스택 50개 상한. 실행취소에 토스트 없음(§9.16)
  - 에이전트가 검증 중 **버그 3개를 스스로 발견해 수정**: ①`docRef.current`가 한 틱 뒤처져 영역 채우기의 undo가 누락되던 문제 ②`ToolRail`의 R키 가드가 오래된 `activeTool`을 참조하던 문제 ③휠 팬/줌 중 고스트가 커서를 따라오지 않던 문제
  - R키 충돌 해결: §9.6은 R=영역 채우기, FR-3.4는 R=회전 → **스탬프 도구일 때만 R이 회전**으로 동작
  - 커밋 `4a71058`
- [x] **M1-4a 드래그 보간 + 미구현 P0 3종** (2026-09-05) — Opus가 육안으로 결함 발견 후 지시
  - **발견한 결함**: 캔버스를 빠르게 드래그하면 칸을 건너뜀(5칸 중 2번째가 빠짐). `toolInteractions.ts` 주석에도 "보간하지 않는 단순화"로 남아 있었음
  - 해결: mm 좌표에서 25mm(피치의 절반) 간격으로 선형 보간. 타일·지우개·엣지 조작 전부 적용. 대각선 점프는 L자 우회 없이 건너뜀(의도치 않은 선 방지)
  - **FR-3.2 팔레트→캔버스 드롭** 구현(팔레트는 `dragstart`를 보내고 있었으나 받는 쪽이 없었음)
  - **FR-3.8 Shift 자유 배치** 구현 — Shift면 격자 무시하고 `props`에 mm 좌표로 배치. `drawPropsLayer` 신설
  - 에이전트 실측: 4칸을 건너뛰는 한 번의 `pointermove`로도 **5칸 전부 칠해짐**(`getImageData`로 5개 셀 중심 샘플링). 엣지도 4구간 전부 이어짐. 대각선 점프 시 엣지 미생성 확인. 각 동작이 실행취소 1번에 되돌아감
  - 커밋 `61f5df3`
  - `Prop.flip` 필드를 스키마에 추가(§5에 없었음, 선택 필드라 하위 호환)

- [x] **M1-5a 텍스트·마커 도구 + 선택 히트테스트** (2026-09-06)
  - FR-4.1/4.2 텍스트 라벨 — T 클릭으로 생성, 캔버스 위 인라인 `<input>`으로 바로 입력. **선 위 흰 글씨 모드**(공식 자료 `+2`/`-3` 스타일)는 얇은 검은 외곽선 + 흰 글자로 구현
  - FR-4.3/4.4 마커 — M 클릭=출발(같은 자리 반복 클릭 시 방향 N→E→S→W 순환), Alt+클릭=도착 토글. 출발/도착은 같은 노드에 공존 불가
  - **마커는 인쇄되는 그림**이므로 mm 실치수·흑백 모양 구분(출발=빈 원+방향 삼각형, 도착=과녁 겹원). PRD §9.12와 충돌 → 아래 정정 참조
  - V(선택) — `hitTest.ts` 신규. 라벨→프롭→셀 역순 검사, 회전 사각형 판정. Delete/Backspace 삭제
  - 스토어에 `commitDoc`(단발 편집용 실행취소 통로) 추가
- [x] **M1-5c 시작 화면(§9.8)** (2026-09-06)
  - 프리셋 카드 5종. 썸네일은 **실제로 맵을 렌더한 그림**(§9.8 "아이콘이나 글자로 대체하지 않습니다")
  - 파일 드롭존 + 창 어디에 놓아도 열리는 전역 드롭 핸들러(FR-1.3)
  - 초안 복구 배너(§9.16), 상단바 뒤로가기 연결(미저장 시 확인 모달)
  - 프리셋 3종(부록 배리어프리·재난구조·라인트레이서)은 자유곡선(FR-10) 필요 → M1.5로 보류
- [x] **M1-5b 손질** (2026-09-06) — Opus 판단
  - 빈 글자로 확정한 라벨을 문서에 남기지 않고 삭제(유령 항목 방지). 이때 생성 시 쌓인 실행취소 한 칸도 걷어내 `dropLastUndoSnapshot` 추가 — Ctrl+Z 한 번에 라벨이 사라지도록
  - 라벨 입력창이 팬·줌을 따라가도록 수정(팬은 리렌더를 안 일으켜 입력창이 붙박여 있었음)
  - 선택 핸들 6px → **8px**(PRD §9.12 수치)
  - **PRD §9.12 정정**: 출발·도착 마커를 화면 전용 오버레이 표(28px 원·색 구분)에서 빼고 인쇄되는 mm 규격으로 옮김. 오버레이 표에는 배치 고스트만 남김

- [x] **M1-5d 검증 로직(FR-9)** (2026-09-06)
  - `src/lib/geometry/validate.ts` — 격자를 노드·엣지 그래프로 보고 BFS(`reachableNodes`). 정답 코드 생성(FR-8)도 같은 그래프를 재사용할 예정
  - 검사 4종: 도달 불가(error) / 출발점 없음(warn) / 규격 이탈 50mm·8mm(warn) / 프롭이 검은 선을 덮음(warn)
  - **엣지가 하나도 없는 칸은 신고하지 않음** — "빈 격자" 프리셋을 열자마자 20건이 쏟아지면 검증 섹션이 무의미해짐
  - 목록은 최대 10건 + 요약 1건으로 자름(항목당 44px라 그 이상은 인스펙터가 스크롤만 하게 됨)
  - FR-9.4(겹침 여백이 선 침범)는 출력 계획기가 있어야 판정 가능 → M3으로
- [x] **M1-5e 인스펙터(§9.13)** (2026-09-06)
  - 4개 섹션: 선택 항목(타일/프롭/라벨/곡선) · 맵 설정 · 용지 · 검증
  - **격자 리사이즈 시 (열,행) 좌표 기준 재배치** — row-major 인덱스가 cols에 의존하므로 단순 slice면 타일이 전부 밀림. 범위 밖 edges·stubs·markers도 함께 정리
  - 검증 항목 클릭 → 캔버스가 그 위치로 이동 + 0.6초 깜빡임(`focusRequest` + nonce). `Viewport.centerOn()` 추가
  - 인스펙터를 접어도 경고가 있으면 접기 버튼에 6px 점 배지
  - 라벨 색 스와치 6종은 BT.601 휘도를 계산해 흑백 복사에서도 구분되게 선택
- [x] **M1-5f 손질** (2026-09-06) — Opus 판단
  - **포인터 캡처 실패가 클릭 전체를 삼키던 버그 수정.** `setPointerCapture`가 도구 동작보다 먼저 호출돼, 예외가 나면 그 아래 `handlePointerDown`이 통째로 실행되지 않았음. 캡처는 "드래그가 캔버스 밖으로 나가도 이어지게 하는" 편의 기능일 뿐이므로 실패해도 도구 동작은 진행하도록 try/catch
  - **개발 서버가 IPv6에만 바인딩되던 문제 수정** (사용자가 크롬에서 흰 화면 신고). `host` 미지정 시 Node 17+ 는 localhost 주소를 재정렬하지 않아 윈도우에서 `::1`에만 붙음 — 실측으로 `127.0.0.1:5173` 연결 실패 / `[::1]:5173` 200 확인. `host: true`로 양쪽 바인딩

- [x] **M1-5g 흰 화면 원인 수정** (2026-09-06) — 사용자 신고, Opus 진단
  - **광고 차단 확장이 타일 파일을 막아 앱이 아예 안 뜨던 문제.** 모험 테마 타일 7종이 `adv-book.png` 처럼 `adv-` 로 시작했는데, uBlock Origin 등이 이를 광고(advertisement)로 보고 요청을 차단(`net::ERR_BLOCKED_BY_CLIENT`)
  - `catalog.ts`의 `import.meta.glob`이 `eager: true`라 개발 서버에서는 png가 하나하나 **모듈**로 불려옴 → 하나만 막혀도 모듈 그래프 전체가 실패 → **흰 화면**
  - `adv-*` → `adventure-*` 로 이름 변경(테마 키 `adventure`와도 일치). `catalog.ts`에 금지 낱말 목록 주석 추가 — 학교 컴퓨터에는 광고 차단기가 흔함
  - 개발 서버 `base`를 `'/'`로 (배포 빌드만 `/hamsterCreator/`). 개발 중에는 하위 경로가 쓸모없이 주소만 길게 만들었음
  - 앞선 IPv6 전용 바인딩 수정(M1-5f)은 별개의 실제 결함이었고 그것도 유효함

- [x] **M1-5h 장애물 타일 렌더 순서 수정** (2026-09-06) — 사용자 발견
  - `cells`를 저장 형식 변경 없이 `TileKind`로 나눠 렌더: `floor`·`block`은 기존 `art` 레이어(격자선 아래), `object` 12종은 `props` 레이어 첫 순서(격자선 위)
  - 합성 순서: 흰 배경 → 바닥·블록 → 격자선 → 낱개 물건 → 자유 배치 props → labels → markers. PRD §5와 `types.ts` 주석도 같은 내용으로 정정
  - 타일 디코드 완료 시 `art`와 `props`를 모두 dirty 처리. object 셀뿐 아니라 어떤 타일이든 Shift 자유 배치 prop으로 쓰일 수 있어 두 레이어 모두 갱신해야 함
  - `npm run build` 성공: JS 292.86kB(gzip 98.81kB), CSS 27.91kB
  - 브라우저 시각 검증: 완전 격자 첫 칸의 보물상자는 검은 선을 가리고, 인접 칸의 돌바닥은 검은 선 아래에 유지됨

- [x] **M1-5i 최초 코치 마크 + 캔버스 빈 상태** (2026-09-06)
  - PRD 전체를 다시 읽고 §9.15·9.16, S7, NFR-9·10을 기준으로 구현
  - 최초 편집기 진입 시 현재 팔레트의 첫 타일 오른쪽에 `타일을 고르고 칸을 클릭하세요` 코치 마크 1개 표시. 타일 선택만으로는 사라지지 않고 실제 배치 성공 때만 제거
  - 클릭·드래그·영역 채우기·우클릭·팔레트 드롭·Shift 자유 배치 경로에서만 `tilePlacementNonce`를 올림. 선·라벨·지우개 편집은 첫 배치로 오인하지 않음
  - 첫 배치 후 `hamsterS.coach.tilePlaced.v1` localStorage 플래그를 저장해 새 탭·새 맵에서도 다시 표시하지 않음. localStorage가 막힌 대피로에서는 현재 화면에서만 숨김
  - `cells`와 `props`가 모두 비었을 때 아트보드 중앙에 `왼쪽에서 타일을 골라 칠해 보세요`를 화면 전용 overlay에 표시. 첫 배치 직후 제거되며 문서·미리보기·PDF 데이터에는 포함되지 않음
  - `npm run build`, `npm run build:single` 성공. 단일 HTML 1,969.71kB(gzip 1,331.10kB)
  - 브라우저 검증: 최초 두 안내 표시 → 타일 선택 시 유지 → 첫 배치 시 둘 다 제거 → 새 탭 재방문 시 코치 마크 미표시

- [x] **M1-5j 내 이미지 빈 상태 + 좁은 화면 팔레트** (2026-09-06)
  - PRD §9.15: 내 이미지가 없을 때 72px 드롭존 축소판과 하단 `이미지 추가` 버튼을 함께 표시. 드롭존은 클릭 파일 선택과 이미지 파일 드롭을 같은 업로드 처리로 연결하고 드래그 중 강조 상태 제공
  - PRD §9.17: 1080px 미만에서 팔레트 280→240px, 3열 타일 77→65px 적용. 기기 배율 때문에 1079px 요청이 1079.2 CSS px로 계산되는 경우까지 포함하도록 경계를 `1079.98px`로 지정
  - 좁은 팔레트에서 최초 코치 마크가 잘리지 않도록 첫 타일 오른쪽 가장자리에 2px 겹쳐 배치
  - 브라우저 실측: 1079px에서 팔레트 240px·타일 65×65px, 1080px에서 팔레트 280px, 1024px에서 드롭존 높이 72px·인스펙터 자동 접힘. 1024px 코치 마크 전체가 팔레트 안에 들어옴
  - `npm run build`, `npm run build:single` 성공. 단일 HTML 1,971.24kB(gzip 1,331.42kB)

- [x] **M1.5a 자유곡선 P/D 생성·렌더·선택** (2026-09-06)
  - P 곡선 펜: 클릭으로 정점을 쌓고 `Enter` 또는 더블클릭으로 한 번에 확정. `Esc` 취소, `Backspace` 마지막 정점 제거. 확정 전에는 primary 색 경로와 7px 정점으로 미리보기
  - D 자유 그리기: 드래그 궤적을 0.5mm 간격으로 수집한 뒤 Douglas-Peucker(허용오차 2mm)로 단순화해 spline으로 저장
  - 공용 `strokeGeometry.ts`: Catmull-Rom 샘플링, 화면 경로 렌더, 선까지의 거리, 경계, Douglas-Peucker를 한곳에 두어 향후 PDF·검증과 같은 기하를 재사용 가능
  - 실제 mm 선폭(기본 8mm), 검정, round 끝·이음으로 `strokes → edges` 순서 렌더. 기존 MapDoc 스키마 변경 없음
  - V 선택 히트테스트에 곡선 추가: object 셀 → 곡선 → floor/block 셀 순서로 렌더 역순을 반영. 선택 시 경로와 7px 정점 표시, Delete/Backspace 삭제 지원
  - 곡선 생성·삭제는 각각 실행취소 1단계. 실행취소로 선택 대상이 사라질 때 유령 선택 오버레이가 남던 문제를 검증 중 발견해 문서 변경 시 선택 유효성 재동기화
  - 브라우저 검증: P 3정점 곡선 생성(153mm 표시), D 드래그가 2정점으로 단순화, V 재선택, 삭제·실행취소, 유령 선택 제거, 오류/경고 로그 0건
  - `npm run build`, `npm run build:single` 성공. 단일 HTML 1,977.64kB(gzip 1,333.20kB)

- [x] **M1.5b O 도형·트랙 프리셋(FR-10.3·10.7)** (2026-09-06)
  - Stroke 판별 유니온에 `roundedRect`(`cx/cy/w/h/radius`)를 추가하고 PRD §5 예시 스키마도 동기화. 공용 기하에서 경로 샘플링·Canvas 렌더·히트테스트·경계·둘레 계산 지원
  - O 버튼을 클릭하면 마지막으로 고른 도형을 사용하고, 450ms 길게 누르거나 우하단 삼각 버튼을 누르면 우측 하위 메뉴(직선·원·타원·라운드 사각)가 열림
  - 캔버스 드래그로 도형 크기를 지정하고 드래그하지 않은 클릭은 기본 크기(직선 60mm, 원 Ø100mm, 타원/라운드 사각 100×70mm)로 배치. 확정 전 primary 반투명 미리보기, 확정 후 선택, 제스처당 실행취소 1단계
  - 인스펙터 직접 입력: 직선 시작/끝 XY, 원 중심/직경, 타원 중심/가로/세로, 라운드 사각 중심/가로/세로/모서리 반경. 타이핑 중에는 로컬 draft만 바꾸고 blur/Enter 때 한 번만 커밋
  - 트랙 팔레트 10종: 직선 270mm, 원 Ø100/130/150, 타원 230×150, S/U 곡선, 미로형, 60mm 좌/우 직각. 흰 배경의 실제 형상 SVG 썸네일 클릭 시 말판 중앙에 실측 크기로 배치하고 선택 도구로 전환
  - 실측 크기를 임의 축소하지 않으므로 A4 5×4보다 큰 프리셋은 일부가 말판 밖에 놓이며 토스트로 안내. 토스트에서 즉시 실행취소 가능
  - 브라우저 검증 중 도형 메뉴가 팔레트 뒤에 잘리는 문제를 발견해 rail overflow와 z-index 수정. 실제 브라우저에서 하위 메뉴 선택, 라운드 사각 드래그, 직경 120mm 입력, 트랙 10종 노출, Ø100 중앙 배치·실행취소 활성화를 확인
  - `npm run build`, `npm run build:single` 성공. 단일 HTML 1,988.71kB(gzip 1,336.08kB)

- [x] **M1.5c 정점 편집·곡률/평행 간격 검증(FR-10.4·10.8·10.9)** (2026-09-06)
  - V 선택 도구에서 spline/line 정점을 직접 드래그해 이동. 드래그 전체를 실행취소 1단계로 묶고, 클릭만 한 경우에는 문서·실행취소 스택을 바꾸지 않음
  - 선택 경로 더블클릭으로 실제 곡선 위에 정점을 삽입하고, 정점 Alt+클릭 또는 활성 정점 Delete/Backspace로 제거. 최소 2정점은 유지
  - spline 정점을 활성화하면 3px 베지어 핸들과 1px 연결선을 표시. 핸들 드래그 시 반대 핸들을 대칭 이동해 매끄러운 접선을 유지
  - `SplineStroke.handles?`를 정점 기준 상대 벡터로 추가. 필드가 없는 기존 파일은 Catmull-Rom 자동 핸들을 사용해 기존 곡선 모양을 유지하며 PRD §5 D10도 동기화
  - 공용 기하를 Catmull-Rom과 동치인 cubic Bezier 샘플링으로 통합해 화면·히트테스트·길이·검증이 사용자 지정 핸들을 동일하게 반영
  - 최소 곡률 반경 50mm 검증: 원·타원·라운드 사각은 해석값, spline은 표본 3점 외접원 반경으로 판정하고 가장 작은 위치를 경고
  - 평행 구간 간격 검증: 10도 이내로 5mm 이상 나란한 표본 구간의 선 가장자리 간격이 40mm 미만이면 경고. 같은 인접 곡선 조각은 제외하고 stroke 쌍당 1건으로 제한
  - 브라우저 검증: S 곡선 정점 드래그로 길이 217→268mm 변경, 실행취소 1회로 217mm 복구, 베지어 핸들 드래그로 곡선 형태·곡률 경고 갱신, 겹친 직선 2개에서 평행 간격 0mm 경고 확인
  - `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M2 단일장 PDF 출력(FR-6.1·6.5·6.6·6.9·6.10·6.11)** (2026-09-06)
  - `pdf-lib`와 `@pdf-lib/fontkit` 기반으로 A4~A0 단일장 PDF를 생성. 용지 mm·방향을 실제 PDF pt로 변환하고 맵을 페이지 중앙에 실물 크기로 배치하며, 페이지를 넘는 맵과 `나눠 인쇄`는 사용자 토스트 오류로 중단
  - 렌더 순서를 바닥·블록 → 격자/진입로/교차점 → 오브젝트 → 자유 배치 → 라벨 → 출발·도착 마커로 고정. 셀·사용자 이미지는 원본 PNG 바이트를 임베드하고, 아이콘만 600px PNG로 변환
  - 선·스플라인(사용자 베지어 핸들 반영)·원·타원·라운드 사각을 PDF 벡터 경로로 생성하고 맵 클리핑 적용. 회전·좌우 반전 셀 이미지도 페이지 좌표계에서 보존
  - 모든 페이지에 `※ 실제 크기(100%)로 인쇄하세요` 안내와 50mm 눈금자를 표시. TopBar `인쇄`에서 비동기 생성·중복 클릭 방지·성공/오류 토스트·파일 다운로드 연결
  - SP-7: 기존 WOFF2 3,728자 서브셋과 `subset:true`는 Poppler에서 한글이 공백/부분 글리프로 렌더됨. PDF 전용 공식 Pretendard SemiBold 전체 TTF를 `subset:false`로 임베드해 임의 한글 라벨을 안정적으로 출력하도록 결정(파일 크기 증가 위험)
  - 구조 QA: 1페이지 A4, 50mm 눈금자, 벡터 베지어 제어점, 원/라운드 사각 경로 확인. 150dpi Poppler 렌더에서 안내문·`햄스터S 한글 PDF 검증`·격자·곡선·눈금자 시각 확인
  - 브라우저 QA: A4 기본 프리셋 인쇄 성공 토스트 및 A4 2장(500×200mm)의 A4 실물 크기 초과 오류 토스트 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M3 출력 계획기 + 타일링(FR-5, FR-6.2~6.8, §9.14)** (2026-09-06)
  - 모든 지원 용지·가로/세로 후보에 대해 셀 수용량, 시트 수, 이음매 수, 낭비 칸을 계산하고 `장수 최소`·`이음매 최소`·`낭비 최소` 정렬 제공. 1장 대안을 자동 추천하고 낭비 0·이음매 0 조합 강조
  - 최소 장수를 유지하는 셀 경계 분할 중 자유곡선 교차 수가 가장 적은 조합을 동적 계획법으로 선택. 동일 점수에서는 앞 시트를 최대한 채우고, 피할 수 없는 교차는 옵션·미리보기·검증 목록에서 경고
  - 940×640 출력 계획기 모달: 격자/실물 크기 입력, SVG 분할 도식, 실제 맵 썸네일, 전체 시트 미리보기, 추천 카드, 맞대기/겹치기 및 겹침 mm 입력, 생성 중 진행 상태 구현. 좁은 화면에서는 옵션을 가로 스크롤하고 이음매 설정을 두 줄로 배치
  - 타일 PDF는 셀 경계로 맵을 클리핑하고 원본 이미지·벡터 곡선 렌더를 재사용. 각 시트에 점선 재단선, 모서리 재단 마크, 변 중앙 삼각 정렬 마크, `행-열` 번호, 이웃 방향, 50mm 눈금자, 100% 안내문을 표시
  - 겹치기는 오른쪽·아래쪽 이웃 영역을 지정 mm만큼 포함하며 용지를 넘으면 생성 전 오류. 마지막에 A4 조립 안내도 1장을 추가해 전체 시트 배치와 자르기/붙이기 순서를 제공
  - 결정형 QA: A4 10×8 → 2×2장·이음매 4·낭비 0·절단 [5]/[4], 11×4 곡선 회피 맵 → 절단 [4,9]·교차 0 확인
  - PDF QA: A4 4장 + 조립 안내도 = 5페이지, 1,265,275바이트. Poppler 110dpi 렌더에서 타일 내용·한글·재단선·정렬 마크·시트 번호·이웃 방향·50mm 눈금·조립 안내도 확인
  - 브라우저 QA: 10×8 크기 입력, A4 4장 옵션 선택·미리보기, 맞대기 PDF 다운로드 성공, 상단 인쇄→계획기 연결, B4 10×10·겹치기 10mm 용지 초과 오류 토스트, 좁은 화면 이음매 설정 줄바꿈 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4 정답 생성(FR-7)** (2026-09-06)
  - 격자 엣지를 공용 인접 그래프로 변환하고 BFS로 출발→도착 최단 경로를 생성. 복수 도착점은 모달의 위/아래 버튼으로 경유 순서를 정하며 각 구간 최단 경로를 연결
  - 출발 방향 N/E/S/W를 반영해 `board_left()`·`board_right()`로 회전한 뒤 `board_forward()`로 한 칸 이동하는 실행 가능한 Python 코드를 생성. 180도는 90도 우회전 2회로 표현하고 이동 칸·회전 횟수 집계
  - 출발점 없음·도착점 없음·도달 불가를 구분하고, 도달 불가는 목표에 가장 가까운 도달 가능 노드의 `캔버스에서 보기` 강조로 연결
  - 상단바 `정답` → 900px 모달 연결. 최단 경로 SVG 도해·코드·통계·코드 클립보드 복사·복수 도착 순서·오류 상태 및 720px 이하 단일 열 반응형 UI 구현
  - 자유곡선은 화면/PDF/검증과 동일한 샘플링으로 트랙 근사 길이를 계산하고 `line_speed(5)`·`line_gain(5)`·`line_both()` Python 템플릿 제공
  - A4 정답지 PDF: 최단 경로 도해 + 도착 번호 + 이동/회전 통계 + 코드 페이지, 자유곡선 도해 + 근사 길이 + 템플릿 페이지 생성. PDF 폰트 로더를 공용화해 단일 HTML의 2.7MB TTF 중복 인라인 제거
  - 공식 Hamster-S Python API Wiki에서 `board_forward()`=한 칸 전진, `board_left/right()`=제자리 90도 회전임을 확인
  - 결정형 QA 15건: 정상 최단 경로·복수 도착 순서·90/180도 회전·도달 불가 마지막 지점·원 둘레 길이 통과
  - 브라우저 QA(고정 495px): 출발점 없음 오류·정상 3칸/2회 경로·코드 복사 토스트·정답 PDF 다운로드·원 Ø100mm 31.4cm 라인트레이싱 탭·단일 스크롤 반응형 확인. QA로 추가한 마커·트랙은 실행취소로 원복
  - PDF QA: 격자+원 혼합 답안 4페이지, 1,257,391바이트, A4. Poppler 110dpi 렌더에서 한글·보라 경로·도착 번호·격자 코드·원 도해·31.4cm·라인 템플릿 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single` 성공. package.json에 별도 test 스크립트는 없어 Vite SSR 단언으로 관련 로직 검증

- [x] **M4a 도구별 팔레트 + 출발·도착 UX 개편** (2026-09-06) — 사용자 사용성 피드백
  - 팔레트를 현재 도구의 설정·안내 공간으로 변경. 타일 목록은 타일 배치/영역 채우기에서만 보이고, 선택·선·스포이드·지우개·글자·곡선 도구는 캔버스에서 할 다음 조작을 표시
  - 테마 가로 스크롤을 제거하고 던전·숲·얼음·모험·사탕·공룡·아이콘·내 이미지를 한 번에 고르는 드롭다운으로 교체. 영역 채우기에서 타일을 골라도 채우기 도구가 유지됨
  - 출발/도착 선택과 출발 방향 N/E/S/W를 팔레트 카드로 명시. 숨은 Alt+클릭 의존 제거, 도착 복수 배치·같은 위치 재클릭 삭제 유지
  - 도형 4종과 트랙 프리셋 10종을 도형 팔레트에 노출해 길게 누르기/작은 삼각 버튼 의존 제거
  - 출발·도착 마커가 격자선에 뚫려 보이지 않도록 흰 배경으로 선을 가리고 방향 삼각형을 축소. 화면과 지도 PDF 렌더를 동일하게 변경
  - 브라우저 QA: 도구별 내용 전환, 채우기 선택 유지, 오른쪽 방향 출발·도착 배치, 도형 4종·트랙 10종, 495/1024/1080/1180/1440px 확인. 가로 오버플로 및 오류 로그 0건
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4b 아이콘·사용자 이미지 캔버스 렌더링 수정** (2026-09-07) — 투명 배치 버그
  - 캔버스 캐시가 PNG 타일 35종만 해석하던 원인을 수정해 자체 SVG 아이콘 8종과 문서의 `asset:*` 사용자 이미지까지 같은 캐시에서 처리
  - viewBox만 있는 SVG는 Chromium이 Blob을 직접 `ImageBitmap`으로 만들지 못하므로 `HTMLImageElement → 600×600 중간 캔버스 → ImageBitmap` 순서로 변환. 투명 배경과 PDF의 아이콘 해상도 기준을 유지
  - 다른 파일에서 같은 `asset:u1` id가 새 내용으로 바뀌는 경계 상태는 원본 data URL까지 비교하고, 늦게 끝난 이전 디코드 결과를 폐기해 이미지가 뒤바뀌지 않게 함
  - 자산 준비 완료 시 셀 아트·오브젝트·배치 고스트 레이어를 함께 갱신. 아이콘 셀은 PDF·선택 히트테스트와 동일하게 격자선 위 오브젝트로 분류
  - 브라우저 QA(495px): 아이콘 셀 표시, Shift 자유배치 후 프롭 인스펙터 선택, 새로고침 후 내장 아이콘 8종 디코드 오류 0건 확인. 초기 SVG 직접 디코드 실패 상태에서도 앱이 중단되지 않고 다른 자산은 유지됨
  - 실제 파일 선택을 동반한 사용자 이미지 UI 검증은 수행하지 않았으나, 팔레트가 만드는 PNG data URL을 문서에서 지연 디코드하는 코드 경로와 교체 경쟁 방지 로직을 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4c 오브젝트 타일 렌더링 분류 수정** (2026-09-08) — 격자선 관통 버그
  - 캔버스·PDF 렌더러는 이미 `object` 타일을 격자선 위에 그렸지만, 최초 추출 때 가장자리 불투명도를 기준으로 분류한 탓에 배경을 가득 채우는 낱개 물건이 `floor`·`block`으로 잘못 기록된 것이 원인이었음
  - 보물상자·나무 상자·나무 빗장·펭귄·책·두루마리·초콜릿 7종을 의미에 맞게 `object`로 재분류. 최종 분류는 floor 9종·block 7종·object 19종
  - 저장 형식이나 렌더러를 바꾸지 않고 카탈로그 데이터만 수정해 화면·히트테스트·단일장/타일 PDF가 같은 분류를 공유하도록 유지
  - 브라우저 QA(206%): 보물상자 내부를 격자선이 관통하지 않음, 선택 후 인스펙터 표시, 실행취소 정상, 콘솔 오류·경고 0건 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4d 용지 계열 묶기 + 맵 설정 반응형 개선** (2026-09-08)
  - 용지 드롭다운을 A 계열(A4·A3·A2·A1·A0)과 B 계열(B4·B3·B2·B1)로 묶고, JIS B1 1030×728mm를 추가. 50mm 피치 기준 가로 20×14칸·활동면적 1000×700mm·여백 좌우 15mm/상하 14mm
  - `PaperSize.series`를 단일 분류 기준으로 추가해 인스펙터만 별도 목록을 하드코딩하지 않도록 함. 출력 계획기·단일장/타일 PDF·상단 규격 칩은 기존 `PAPER_SIZES` 경로로 B1을 자동 지원
  - 맵 설정을 열·행 / 피치·선폭의 2×2 입력으로 정리하고, CSS Grid의 암묵적 `min-content` 때문에 두 번째 열이 잘리던 문제를 `minmax(0, 1fr)`로 수정. 규격 이탈 경고는 두 입력 아래 전체 폭으로 분리
  - 세그먼트 버튼을 내용 폭 기준으로 배분하고 줄바꿈을 금지해 `나눠 인쇄`가 두 줄로 꺾이지 않게 함. 4항목 회전 컨트롤처럼 폭이 큰 행은 인스펙터 안에서 다음 줄로 감쌀 수 있게 함
  - 브라우저 QA: 1024px에서 맵 설정 섹션 scrollWidth 468→267px, 문서 가로 오버플로 0, A/B optgroup과 B1 선택·실행취소, 1440px 레이아웃, 콘솔 오류·경고 0건 확인
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4e S 곡선 프리셋 곡률 경고 해소** (2026-09-08)
  - 기존 5개 정점은 양끝의 평평한 구간에서 대각 전환 구간으로 급격히 이어져 현재 검증 방식에서 최소 곡률 반경이 약 26.40mm로 측정됐음
  - 검증 기준 50mm는 유지하고, 프리셋을 사인 곡선에 가까운 대칭 7개 정점으로 조정. 가로 폭 180mm와 S 형태를 유지하면서 세로 높이를 90→80mm로 완만하게 줄임
  - 변경 좌표의 최소 곡률 반경은 동일한 16분할 표본·외접원 방식에서 약 67.39mm이며, 선폭을 포함한 경계 약 188×88mm가 기본 A4 5×4 말판 안에 들어감
  - 사용자 요청에 따라 최소 검증만 수행: `npx tsc -b --pretty false` 및 곡률 수치 확인

- [x] **M4f U 곡선·공식 타원 곡률 검증 정합성 해소** (2026-09-09)
  - U 곡선 좌표를 형태 비율 그대로 1.2배 확대해 최소 곡률 반경을 약 43.04→51.65mm로 개선. 선 포함 약 200×146mm라 기본 A4 5×4 말판 안에 유지
  - FR-10.7의 공식 `타원 230×150mm`는 해석상 최소 곡률 반경이 약 48.91mm라 FR-10.8의 50mm와 충돌함. 공식 크기와 전체 기준을 모두 보존하고 정확히 230×150mm인 타원만 명시적 예외로 처리
  - 공식 타원 치수를 공용 상수로 분리해 프리셋과 검증의 값이 어긋나지 않게 했고, 사용자가 치수를 바꾸면 즉시 일반 50mm 검증으로 복귀
  - Vite SSR 결정형 QA: U·공식 타원 경고 0건, 세로 148mm 비공식 타원과 반경 49mm 원은 곡률 경고 발생
  - 브라우저 QA(495px): U 배치·공식 타원 배치에 곡률 경고 없음, 타원 세로 148mm 변경 시 약 48mm 경고 표시, 콘솔 오류·경고 0건
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4g 도착점 사용자 이름 보존** (2026-09-09)
  - 도착점 추가·삭제 시 기존 `name`을 다시 쓰지 않고, 빈 이름만 화면에 표시할 때 `도착` 또는 `도착1`·`도착2`로 계산하도록 공용 규칙을 분리
  - 사용자 이름과 자동 이름이 겹치면 사용하지 않은 가장 작은 번호를 선택하며, 캔버스·지도 PDF·정답 목록·도달 불가 오류가 모두 같은 표시명을 사용
  - 정답 경유 순서를 바꿔도 도착점의 위치별 표시명이 바뀌지 않도록 문서 순서 기반 이름표를 적용하고, 남아 있던 Alt+클릭 안내 문구를 현재 마커 팔레트 조작으로 수정
  - Vite SSR 결정형 QA: 단일/복수 빈 이름, 사용자 이름 혼합, 이름 충돌, 순서 변경, 사용자 이름 도달 불가 오류 통과
  - 브라우저 QA: 두 도착점 `도착1`·`도착2` 표시, 정답 경유 순서 변경 후 이름 위치 고정, 콘솔 오류·경고 0건
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4h 도착점 이름 입력·수정 UI(FR-4.4)** (2026-09-09)
  - 출발·도착 팔레트의 `도착지` 모드에 배치된 도착지별 표시명·열·행과 이름 입력 목록을 추가. 도착지가 없을 때는 캔버스에 먼저 놓으라는 빈 상태 안내 표시
  - 타이핑 중에는 로컬 draft만 바꾸고 Enter/포커스 이탈 때 한 번만 `commitDoc`을 호출해 실행취소 기록을 한 단계로 유지. Escape는 저장하지 않고 입력 전 값으로 복원
  - 입력값 앞뒤 공백을 정리하고 빈 값은 M4g 자동 표시명으로 복귀. 위치 키로 최신 문서의 대상을 다시 찾아 다른 도착지 추가·삭제로 배열 순서가 달라져도 잘못 수정하지 않음
  - 브라우저 QA: 빈 상태, 복수 도착지 자동 번호, Enter로 `학교` 저장, 포커스 이탈로 `공원` 저장, 새 도착지 추가 후 이름 보존, Escape 취소, 공백 이름 자동복귀 확인
  - 반응형 QA: 같은 브라우저 세션의 1024×720·1440×900에서 입력 3개가 잘림 없이 표시되고 문서 가로 오버플로 0, 콘솔 오류·경고 0건
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

- [x] **M4i 시작 화면 프리셋 8종 완성(FR-1.2)** (2026-09-10)
  - 교과서 p190 배리어프리의 3×3 교차점·경계 연장·거실/화장실/현관 표기와 p185 재난구조의 5×5 구조 격자·원형 가스 밸브 구역·하단 경로·임무 아이콘/라벨을 50mm 피치·8mm 선폭의 편집 가능한 문서로 추가
  - 라인트레이서는 PRD 기준에 따라 격자 없이 기존 안전 S 곡선(최소 곡률 약 67mm)을 배치하고, 시작 화면 카드가 총 8종이 되도록 구성
  - 썸네일 렌더에 셀 오브젝트·프롭·라벨·마커 레이어를 추가하고 비동기 아이콘 비트맵 로드 후 다시 그려 새로고침 직후 아이콘 누락을 방지
  - Vite SSR 결정형 QA: 8종 생성, board/cells 정합성, 배리어프리 엣지·라벨·경계 연장, 재난구조 혼합 엣지·원·아이콘·라벨, 라인트레이서 무격자·곡률 검증 통과
  - 실제 브라우저 QA: 1440×900·1024×720에서 카드 8개와 썸네일 로드, 가로 오버플로 0; 신규 3종 모두 편집기 진입·캔버스 렌더 성공; 아이콘 자산 차단 오류 상태에서도 카드 8개와 화면 유지; 브라우저 오류 0건
  - 교과서 배치처럼 임무 아이콘이 검은 선 위에 놓인 재난구조 프리셋은 `prop-covers-line` 경고가 의도적으로 표시되며, 실제 출력·주행 전 확인이 필요
  - `npx tsc -b --pretty false`, `npm run build`, `npm run build:single`, `git diff --check` 성공

## In Progress
- [ ] **배포 차단 항목 수정 완료(0~5번, 2026-09-16).** 커밋·푸시·배포는 아직 하지 않았습니다. 남은 것은 Next Steps의 "배포 후 수정"과 "사용자 실기 필요", 그리고 검토 블록의 질문 1~5입니다.

## Next Steps

2026-09-16 검토 결과를 중요도순으로 정리했습니다. 위에서부터 처리합니다.

### 배포 차단 — 2026-09-16 전부 수정 완료 (기록 보존용)

0. ~~**맵 파일 저장이 UI에 연결돼 있지 않음**~~ (FR-1.4/1.5 P0, 검토 중 발견해 최우선 처리)

1. ~~**초안 슬롯 덮어쓰기**~~ (2026-09-16 완료) — 한 세션에서 맵 A->B로 갈아타면 A의 초안이 사라짐. 재현 완료.
   `src/lib/storage/draft.ts:37`(세션당 id 1개), `:133`(같은 id 제거 후 삽입)
2. ~~**탭 닫기·새로고침 시 미저장 경고 없음**~~ (2026-09-16 완료) (FR-1.8 / NFR-5, 둘 다 P0).
   `beforeunload` 리스너 0건. 앱 내 뒤로가기 모달만 존재(`TopBar.tsx:120`)
3. ~~**출발·도착 이름표가 검은 선 위 검은 글씨**~~ (2026-09-16 완료) — 화면·인쇄 모두 안 보임.
   `drawBoard.ts:321`, `generateMapPdf.ts:387`(`onLine: false`)
4. ~~**타일 7종이 격자선 아래로 깔림**~~ (2026-09-16 완료) — `block` 분류 전체.
   `drawBoard.ts:128`, `generateMapPdf.ts:229`. `ice-floor`·`ice-footprint`는 바닥이라 예외 처리 필요
5. ~~**공개 저장소 권리 정리**~~ (2026-09-16 완료) — `말판/` 공식 PDF 23종 추적 해제 + 내장 아트 35종·아이콘 8종 출처·라이선스 고지 추가

### 배포 후 수정

6. ~~50mm 눈금자·"100% 인쇄" 안내가 프린터 인쇄 불가 여백에 위치~~ (2026-09-16 완료)
   - 파생 항목(재단 마크 잘림)도 2026-09-16 완료. 변마다 바깥/안쪽을 따로 판단하도록 수정.
     남은 한계: 재단선 자체가 종이 끝 5mm 지점이라 그보다 안쪽으로는 못 옮김 — 실제 인쇄 확인 필요
7. ~~FR-2.4 경계 진입로 추가·제거 UI 미구현~~ (2026-09-16 완료)
   - 기본값: 사용자 결정으로 `createFullGridMap`에 경계 진입로를 넣음 (2026-09-16)
   - 크기 변경: 가장자리를 벗어난 진입로 자동 정리 + 사방 세트는 새 크기로 재생성 (2026-09-16)
8. PWA/오프라인(NFR-7) 미구현
9. ~~`robots.txt`가 dist에 안 들어감~~ (2026-09-16 완료) — 검토 지적 중 "noindex와 반대"는 틀렸음. robots.txt는 도메인 최상위에서만 읽혀 하위 경로에선 무의미하므로 파일 삭제, noindex 메타만 유지
10. ~~개발용 "컴포넌트 카탈로그" 링크가 운영 화면에 노출~~ (2026-09-16 완료) — 링크·라우트 DEV 전용 + lazy로 번들 분리
11. ~~파일을 새로 열어도 직전 문서의 "저장 안 됨" 상태가 남음~~ (2026-09-16 완료, 2번과 함께 `App.openDoc`에서 초기화)
12. ~~`rules.md.txt` 0바이트 빈 파일 추적 중~~ (2026-09-16 완료, 5번과 함께 삭제)
13. ~~워크플로 `cancel-in-progress: true` -> `false`~~ (2026-09-16 완료)
14. ~~파비콘 없음~~ (2026-09-16 완료) — 격자 모양 SVG를 data URI로 인라인
15. JS 단일 청크 1.49MB(gzip 627KB) — 현재 NFR-2는 지키나 여유 감소 (10번에서 카탈로그 17KB 분리)
16. 실행취소가 드래그(수동)/단발(`commitDoc` 자동)로 이원화 — 규율로만 유지

> 구현 중 발견해 함께 고친 것(원래 목록에 없던 항목)
> - 빈 곳을 클릭하면 가장 가까운 격자선이 지워지던 문제 (`nearestEdgeToPoint` 범위 검사 없음) — 7번과 함께 수정
> - 재단 마크가 상하 여백 5mm 시트에서 잘리던 문제 — 6번과 함께 수정
> - `npm run preview`가 base 불일치로 배포본을 못 열던 문제 — 10·14·9·13번과 함께 수정

### 사용자 실기 필요 (자동화로 대체 불가)

17. S2 인쇄물 피치 50mm +-0.5mm 자 실측 (※ 6번 때문에 종이에 눈금자가 없을 수 있음)
18. S3 A4 4장 100% 인쇄·재단·조립 후 격자 연속성 + 로봇 주행
19. S4 생성 파이썬 코드로 실물 햄스터S 완주
20. S6 교과서 p188 도로 재현 후 실물 라인트레이싱
21. S7 처음 쓰는 사람 3명 이상 관찰 (안내 없이 10초 내 첫 타일 배치)
22. S1 교과서 p190 재현 (검토 블록의 질문 1 해결 후)
23. 생성 PDF를 실제로 열어 한글 라벨·조립 안내도 배치 눈으로 확인
24. FR-8.3/8.4 사용자 이미지 업로드 -> 저장 -> 복구 실파일 왕복
25. 실제 마우스로 드래그·드롭·Shift 자유 배치
26. GitHub Settings > Pages > Source = "GitHub Actions" 확인, Actions 실행 이력 확인
27. 학교망 첫 화면 로딩 실측(NFR-2)
28. `말판/` PDF와 내장 아트의 재배포 허용 여부 확인

## Changed Files
- `package.json`, `vite.config.ts`, `tsconfig*.json`, `index.html`, `.claude/launch.json`: 빌드·개발 서버 설정
- `src/main.tsx`: `ToastProvider`로 앱 전체를 감쌈 (M0-3)
- `src/App.tsx`: 시작 화면 ↔ 편집기 전환(로컬 useState), `?catalog` 개발용 라우트
- `src/styles/`: 토큰·리셋·타이포그래피 (Pretendard 서브셋 3종)
- `src/components/`: 기본 컴포넌트 8종 (`Button` `Input` `Segmented` `TabPills` `Tooltip` `StatusChip` `Toast` `Modal`) + `index.ts`
- `src/features/start/`: 시작 화면 — `StartScreen` `presets`(8종) `thumbnail`(전체 실제 레이어 렌더·비동기 자산 갱신)
- `src/features/editor/`: `EditorLayout` `TopBar` `ToolRail` `Inspector` `useMapIssues` `editorStore`
- `src/features/print/`: M3 출력 계획기 모달·도식·반응형 레이아웃
- `src/features/answer/`: M4 정답 경로·코드·도착 순서·라인트레이싱 모달
- `src/features/palette/`: `PalettePanel` (도구별 설정·안내, 종류 드롭다운·타일/아이콘/내 이미지, 도형·트랙, 도착지 이름 입력)
- `src/features/canvas/`: `CanvasViewport` `viewport` `renderer`(5레이어) `drawBoard` `drawOverlay` `gridMath` `hitTest` `toolInteractions` `ruler` `minimap` `tileBitmaps` `cssTokens`
- `src/lib/model/`: `types` `constants` `factory` `serialize`
- `src/lib/storage/`: `FsaStore` `DownloadStore` `draft` (File System Access API + 내려받기 대피로)
- `src/lib/geometry/validate.ts`, `src/lib/geometry/gridGraph.ts`: FR-9 검증 + 공용 격자 그래프/BFS
- `src/lib/answer/generateAnswer.ts`: 최단 경로·방향 명령·라인트랙 길이/코드 생성
- `src/lib/print/plan.ts`: 용지 후보 계산·정렬·셀 경계/곡선 회피 분할
- `src/lib/model/resize.ts`: 출력 계획기·인스펙터 공용 격자 크기 변경
- `src/lib/pdf/generateMapPdf.ts`: M2 단일장 PDF 벡터/이미지 렌더러와 다운로드
- `src/lib/pdf/generateAnswerPdf.ts`, `src/lib/pdf/pdfResources.ts`: M4 정답지 PDF와 공용 한글 폰트 로더
- `src/assets/fonts/Pretendard-SemiBold.pdf.ttf`: PDF 한글 라벨용 전체 Pretendard SemiBold TTF
- `src/lib/tiles/catalog.ts`, `src/assets/tiles/`: 내장 아트 타일 35종 + manifest
- `src/lib/icons/catalog.ts`, `src/assets/icons/`: 인쇄용 아이콘 8종
- `.github/workflows/deploy.yml`: Pages 자동 배포
- `.gitignore`

## Commands Run

아래는 과거 단계별 실행 이력입니다. 현재 HEAD에서 다시 실행한 최신 QA 결과가 아닙니다. 2026-09-14 문서 최신화에서는 소스 조회·`git status -sb`·`git log`·커밋 변경 목록 대조만 수행했고 기능 테스트는 재실행하지 않았습니다.

```text
npm install            → 성공
npm run build          → 성공 (2026-09-06 M1.5c 기준 js 318.65kB / gzip 106.35kB, css 31.07kB)
npm run build:single   → 성공, dist-single/index.html 1,997.04kB / gzip 1,338.78kB (M1.5c 기준)
npm run dev            → http://localhost:5173 (포트 고정, IPv4·IPv6 양쪽 바인딩)
npx tsc -b --pretty false → 성공 (M2 PDF 폰트/렌더러)
npm run build           → 성공 (M2, PDF 전용 TTF 2,671.46kB 포함)
npm run build:single    → 성공 (M2, dist-single/index.html 6,700.90kB / gzip 3,387.88kB)
git diff --check        → 성공
npm run build           → 성공 (M3, js 1,476.83kB / gzip 619.26kB, css 35.91kB)
npm run build:single    → 성공 (M3, dist-single/index.html 6,721.99kB / gzip 3,393.85kB)
npx tsc -b --pretty false → 성공 (M4)
npm run build           → 성공 (M4, js 1,492.95kB / gzip 624.75kB, css 39.93kB)
npm run build:single    → 성공 (M4, dist-single/index.html 6,742.07kB / gzip 3,398.67kB)
```

### 검증 방법 메모
브라우저 자동화 환경이 불안정해서(창 배율이 호출마다 바뀌어 좌표 클릭이 어긋남),
다음 두 가지로 확인했습니다. 앞으로도 같은 방법을 쓰면 됩니다.
- **localStorage 초안 읽기** — `hamsterS.drafts.v1` 에 저장된 실제 문서로 결과를 대조.
  캔버스에 뭘 그렸는지 눈으로 보는 것보다 훨씬 확실합니다
- **`.hsmap.json` 파일을 창에 떨어뜨려 알려진 문서를 주입** — 원하는 상태를 정확히
  만들 수 있고, 덤으로 파일 열기 경로(FR-1.3)까지 같이 검증됩니다

## Risks / Open Questions
- **자산 파일 이름에 광고 차단기가 싫어하는 낱말을 넣지 말 것**: `ad` `ads` `adv` `advert` `banner` `popup` `promo` `sponsor` `analytics` `track`. 개발 서버에서는 타일 png가 모듈로 로드되므로 하나만 차단돼도 앱이 통째로 안 뜸. 배포 빌드에서는 해당 그림만 깨지지만 그것도 수업에서 곤란함. `src/lib/tiles/catalog.ts` 상단 주석 참고
- **완전한 격자에서는 선을 하나 지워도 도달 불가 경고가 안 뜸.** 5×4처럼 사방이 이어진 격자는 모든 선이 사각형 고리 위에 있어 다리(bridge) 간선이 없기 때문. 검증이 반응하려면 좁은 통로·막다른 가지에서 유일한 연결선을 끊어야 함. 버그가 아니라 그래프 특성이지만 "선을 지웠는데 왜 경고가 없지?"라는 의문이 생길 수 있음
- **사람이 직접 한 번 만져봐야 하는 것**: 이 환경의 브라우저 창 배율이 호출마다 바뀌어 좌표 기반 UI 조작 검증이 불안정함. 도구 동작은 합성 이벤트 + `getImageData` 픽셀 샘플링으로 검증했으나, 실제 마우스로 드래그·드롭·Shift 배치를 한 번 확인해보는 게 좋음
- `createFullGridMap`이 경계 진입로(stub)를 만들지 않아, 기본 맵이 공식 playbot 말판처럼 종이 가장자리까지 선이 닿지 않음. 공식 자료는 모든 경계 노드에 진입로가 있음. FR-2.4(진입로 추가·제거, P1)가 구현되면 기본값으로 넣을지 재검토 — 지금 넣으면 지울 방법이 없어서 보류
- 이 환경의 브라우저 측정 API(`getBoundingClientRect` 등)가 간헐적으로 잘못된 값을 반환함(화면은 정상인데 캔버스 크기를 300×150으로 보고). 레이아웃 수치 검증은 스크린샷 + 코드 확인을 병행할 것
- **미결 코드 위험 — 초안 덮어쓰기**: `draft.ts`는 여전히 "브라우저 세션 1회 = 초안 1슬롯"을 사용합니다(2026-09-14 코드 확인). 한 세션에서 맵 A→B로 갈아타면 같은 슬롯을 사용하므로 문서별 키/보존 규칙을 검토해야 합니다. 미래 M1 작업으로 미루거나 해결 완료로 취급하지 않습니다.
- 저장소가 Public이므로 GitHub Pages 무료 배포가 가능합니다. 다만 사용자가 모든 작업을 마친 뒤에 배포하자고 지시했으므로 임의로 푸시하지 마십시오
- SP-6: 자유곡선을 곡선과 겹치지 않게 시트 분할하는 알고리즘의 실용성 — M1.5에서 확인. 실패 시 사용자가 이음매 위치를 직접 지정
- SP-7: Pretendard 서브셋 + `pdf-lib` 폰트 임베드 — M2에서 Poppler 시각 렌더로 확인 완료
- M2 PDF는 `pdf-lib/fontkit`의 한글 서브셋 경로가 복합 글리프를 누락해 전체 PDF 전용 TTF를 임베드함. 현재 샘플 PDF는 약 1.25MB이며, 향후 폰트 엔진 교체 또는 안전한 서브셋 도입 시 용량을 줄일 수 있음
- M3 S3의 코드·PDF 렌더 기준(10×8 → A4 2×2, 시트 경계 셀 단위)은 통과했지만 실제 종이 4장을 100%로 인쇄·재단·조립한 물리 검증은 이 환경에서 수행할 수 없어 사용자 실측이 남아 있음
- M3 모달은 인앱 브라우저의 고정 495px 폭에서 좁은 화면 분기까지 확인했으나, 이 환경에 Chrome/Edge 제어가 없어 1024px 이상 데스크톱 창의 실제 스크린샷 검증은 수행하지 못함
- M4 S4/S6의 코드·PDF·브라우저 검증은 통과했지만 실제 햄스터S가 생성 코드로 완주하고 자유곡선을 주행하는 물리 검증은 장비가 없어 남아 있음
- S7의 신규 사용자 3명 이상 관찰 결과가 기록돼 있지 않습니다. 화면·자동화 QA 통과는 S7 최종 통과가 아닙니다.
- PRD S1에는 재현 시간 기준이 있지만 사용자 작업 규칙은 제작 시간 측정·표시를 금지합니다. 최종 판정 전 사용자 지시와 PRD 정합성을 확인해야 하며, 이번 문서 정리에서 PRD나 성공 기준을 임의 변경하지 않았습니다.
- `.github/workflows/deploy.yml`은 main push로 Pages 배포를 실행하도록 설정돼 있습니다. 로컬 원격 추적 참조와 HEAD가 같다는 사실만으로 배포 성공·실제 서비스 버전을 확정할 수 없습니다. Actions/Pages 확인 전 배포 완료 또는 미배포로 단정하지 않습니다.
- M4 PDF QA 산출물 `tmp/answer-qa`는 정확한 하위 경로만 확인해 삭제를 시도했으나 작업공간 정책이 재귀 삭제를 거부해 남아 있음. 기존 `tmp/pylibs`와 함께 광범위 삭제하지 말 것
- 햄스터S 바닥 센서 2개의 좌우 간격(mm) 미확인 — FR-10.9의 40mm 기준을 정밀화할 때 필요 (M6)
- GitHub 저장소가 Public이어야 Pages 무료. 내장 아트가 저작권 프리라 공개에 문제 없음
